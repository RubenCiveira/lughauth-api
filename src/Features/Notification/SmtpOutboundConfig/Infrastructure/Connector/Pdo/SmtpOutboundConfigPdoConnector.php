<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Notification\SmtpOutboundConfig\Infrastructure\Connector\Pdo;

use Throwable;
use Closure;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlParam;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlTemplate;
use Civi\Lughauth\Shared\Exception\NotFoundException;
use Civi\Lughauth\Shared\Exception\NotUniqueException;
use Civi\Lughauth\Shared\Exception\ConstraintException;
use Civi\Lughauth\Shared\Exception\OptimistLockException;
use Civi\Lughauth\Shared\Exception\NotEmptyChildsException;
use Civi\Lughauth\Features\Notification\SmtpOutboundConfig\Domain\Gateway\SmtpOutboundConfigFilter;
use Civi\Lughauth\Features\Notification\SmtpOutboundConfig\Domain\Gateway\SmtpOutboundConfigCursor;
use Civi\Lughauth\Features\Notification\SmtpOutboundConfig\Domain\SmtpOutboundConfig;
use Civi\Lughauth\Features\Notification\SmtpOutboundConfig\Domain\SmtpOutboundConfigRef;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;
use Civi\Lughauth\Features\Access\Tenant\Domain\TenantRef;
use Civi\Lughauth\Features\Notification\SmtpOutboundConfig\Domain\ValueObject\SmtpOutboundConfigPasswordVO;
use Civi\Lughauth\Shared\Security\AesCypherService;

class SmtpOutboundConfigPdoConnector
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly AesCypherService $cypher,
        private readonly SqlTemplate $db,
    ) {
    }
    public function list(?SmtpOutboundConfigFilter $filter = null, ?SmtpOutboundConfigCursor $sort = null): array
    {
        $this->logDebug("List query for Smtp outbound config");
        $span = $this->startSpan("List query for Smtp outbound config");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function listForUpdate(?SmtpOutboundConfigFilter $filter = null, ?SmtpOutboundConfigCursor $sort = null): array
    {
        $this->logDebug("List query for update of Smtp outbound config");
        $span = $this->startSpan("List query for update of Smtp outbound config");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->queryForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function query(string $query, ?array $params = []): array
    {
        return $this->theQuery(false, $query, $params);
    }
    private function rawQuery(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(false, $query, $params);
    }
    private function queryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theQuery(true, $query, $params);
    }
    private function rawQueryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(true, $query, $params);
    }
    private function theQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make query for entities for Smtp outbound config");
        $span = $this->startSpan("Make query for entities for Smtp outbound config");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): SmtpOutboundConfig => $this->mapper($row)) : $this->db->query($query, $params, fn (array $row): SmtpOutboundConfig => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function theRawQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make raw query for Smtp outbound config");
        $span = $this->startSpan("Make raw query for Smtp outbound config");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): array => $row) : $this->db->query($query, $params, fn (array $row): array => $row);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieve(SmtpOutboundConfigFilter $filter): ?SmtpOutboundConfig
    {
        $this->logDebug("Retrieve query for Smtp outbound config");
        $span = $this->startSpan("Retrieve query for Smtp outbound config");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): SmtpOutboundConfig => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieveForUpdate(SmtpOutboundConfigFilter $filter): ?SmtpOutboundConfig
    {
        $this->logDebug("Retrieve query for update of Smtp outbound config");
        $span = $this->startSpan("Retrieve query for update of Smtp outbound config");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row): SmtpOutboundConfig => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function create(SmtpOutboundConfig $entity, ?Closure $verify = null): SmtpOutboundConfig
    {
        $this->logDebug("Execute insert sql query for Smtp outbound config");
        $span = $this->startSpan("Execute insert sql query for Smtp outbound config");
        try {
            try {
                $this->db->execute('INSERT INTO "notification_smtp_outbound_config" ( "uid", "tenant", "host", "port", "login", "password", "sender_name", "sender_email", "timeout", "use_tls", "max_retries", "retry_delay", "rate_limit", "version") VALUES ( :uid, :tenant, :host, :port, :login, :password, :senderName, :senderEmail, :timeout, :useTls, :maxRetries, :retryDelay, :rateLimit, :version)', [
                     new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'tenant', value: $entity->getTenant()?->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'host', value: $entity->getHost(), type: SqlParam::STR),
                     new SqlParam(name: 'port', value: $entity->isPort(), type: SqlParam::BOOL),
                     new SqlParam(name: 'login', value: $entity->getLogin(), type: SqlParam::STR),
                     new SqlParam(name: 'password', value: $entity->getCypheredPassword($this->cypher), type: SqlParam::STR),
                     new SqlParam(name: 'senderName', value: $entity->getSenderName(), type: SqlParam::STR),
                     new SqlParam(name: 'senderEmail', value: $entity->getSenderEmail(), type: SqlParam::STR),
                     new SqlParam(name: 'timeout', value: $entity->getTimeout(), type: SqlParam::INT),
                     new SqlParam(name: 'useTls', value: $entity->isUseTls(), type: SqlParam::BOOL),
                     new SqlParam(name: 'maxRetries', value: $entity->getMaxRetries(), type: SqlParam::INT),
                     new SqlParam(name: 'retryDelay', value: $entity->getRetryDelay(), type: SqlParam::INT),
                     new SqlParam(name: 'rateLimit', value: $entity->getRateLimit(), type: SqlParam::INT),
                     new SqlParam(name: 'version', value: 0, type: SqlParam::INT)
                ]);
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($entity, true);
                throw $ex;
            }
            $created = $entity->withVersion(0);
            if ($verify && !$verify($created)) {
                $this->db->execute('DELETE FROM "notification_smtp_outbound_config" where "uid" = :uid', [
                  new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR)
                ]);
                throw new NotFoundException('Insertion of not visible values');
            }
            return $created;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function update(SmtpOutboundConfig $update): SmtpOutboundConfig
    {
        $this->logDebug("Execute update sql query for Smtp outbound config");
        $span = $this->startSpan("Execute update sql query for Smtp outbound config");
        try {
            try {
                $result = $this->db->execute('UPDATE "notification_smtp_outbound_config" SET "tenant" = :tenant , "host" = :host , "port" = :port , "login" = :login , "password" = :password , "sender_name" = :senderName , "sender_email" = :senderEmail , "timeout" = :timeout , "use_tls" = :useTls , "max_retries" = :maxRetries , "retry_delay" = :retryDelay , "rate_limit" = :rateLimit , "version" = :version WHERE "uid" = :uid and "version" = :_lock_version', [
                     new SqlParam(name: 'uid', value: $update->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'tenant', value: $update->getTenant()?->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'host', value: $update->getHost(), type: SqlParam::STR),
                     new SqlParam(name: 'port', value: $update->isPort(), type: SqlParam::BOOL),
                     new SqlParam(name: 'login', value: $update->getLogin(), type: SqlParam::STR),
                     new SqlParam(name: 'password', value: $update->getCypheredPassword($this->cypher), type: SqlParam::STR),
                     new SqlParam(name: 'senderName', value: $update->getSenderName(), type: SqlParam::STR),
                     new SqlParam(name: 'senderEmail', value: $update->getSenderEmail(), type: SqlParam::STR),
                     new SqlParam(name: 'timeout', value: $update->getTimeout(), type: SqlParam::INT),
                     new SqlParam(name: 'useTls', value: $update->isUseTls(), type: SqlParam::BOOL),
                     new SqlParam(name: 'maxRetries', value: $update->getMaxRetries(), type: SqlParam::INT),
                     new SqlParam(name: 'retryDelay', value: $update->getRetryDelay(), type: SqlParam::INT),
                     new SqlParam(name: 'rateLimit', value: $update->getRateLimit(), type: SqlParam::INT),
                     new SqlParam(name: 'version', value: ($update->getVersion() ?? 0) + 1, type: SqlParam::INT),
                     new SqlParam(name: '_lock_version', value: $update->getVersion(), type: SqlParam::INT)
                ]);
                if (!$result && $this->db->exists('select "uid" from "notification_smtp_outbound_config" where "uid" = :uid', ['uid' => $update->uid() ])) {
                    throw new OptimistLockException($update->uid() ?? 'no-id', "version: " . ($update->getVersion() ?? 0));
                } elseif (!$result) {
                    throw new NotFoundException($update->uid() ?? 'no-id');
                }
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($update, false);
                throw $ex;
            }
            return $update->withVersion(($update->getVersion() ?? 0) + 1);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function delete(SmtpOutboundConfigRef $ref): bool
    {
        $this->logDebug("Execute delete sql query for Smtp outbound config");
        $span = $this->startSpan("Execute delete sql query for Smtp outbound config");
        try {
            try {
                return $this->db->execute('DELETE FROM "notification_smtp_outbound_config" where "uid" = :uid', [new SqlParam(name: 'uid', value: $ref->uid(), type:SqlParam::STR)]);
            } catch (NotEmptyChildsException $ex) {
                throw ConstraintException::ofError('not-empty', ['uid'], [$ref->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function exists(SmtpOutboundConfigFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for Smtp outbound config");
        $span = $this->startSpan("Execute exists sql query for Smtp outbound config");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function existsForUpdate(SmtpOutboundConfigFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for update of Smtp outbound config");
        $span = $this->startSpan("Execute update sql query for update of Smtp outbound config");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->existsForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function count(?SmtpOutboundConfigFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for Smtp outbound config");
        $span = $this->startSpan("Execute count sql query for Smtp outbound config");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): int => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function countForUpdate(?SmtpOutboundConfigFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for update of Smtp outbound config");
        $span = $this->startSpan("Execute count sql query for update of Smtp outbound config");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row) => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function checkDuplicates(SmtpOutboundConfig $entity, bool $creation): void
    {
        $this->logDebug("Query to check duplicates for Smtp outbound config");
        $span = $this->startSpan("Query to check duplicates for Smtp outbound config");
        try {
            $values = ['tenant' => $entity->getTenant()?->uid(), 'uid' => $entity->uid()];
            if ($this->db->exists('SELECT  "tenant" from "notification_smtp_outbound_config" where "tenant" = :tenant and "uid" != :uid', $values)) {
                throw ConstraintException::ofError('not-unique', ['tenant'], [$entity->getTenant()?->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function filter(?SmtpOutboundConfigFilter $filter, ?SmtpOutboundConfigCursor $sort, bool $count): array
    {
        $this->logDebug("Build query filter of Smtp outbound config");
        $span = $this->startSpan("Build query filter of Smtp outbound config");
        try {
            $join = '';
            $query = '';
            $params = [];
            $order = '';
            $limit = '';
            if ($filter) {
                $filterUids = $filter->uids();
                if (null !== $filterUids && count($filterUids) > 1) {
                    $query .= ' and "notification_smtp_outbound_config"."uid" in (:uids)';
                    $params[] = new SqlParam(name:'uids', value: $filterUids, type: SqlParam::STR);
                } elseif (null !== $filterUids) {
                    $query .= ' and "notification_smtp_outbound_config"."uid" = :uid';
                    $params[] = new SqlParam(name:'uid', value: $filterUids[0], type: SqlParam::STR);
                }
                $filterSearch = $filter->search();
                if (null !== $filterSearch) {
                    $query .= ' and ( "notification_smtp_outbound_config"."uid" like :search)';
                    $params[] = new SqlParam(name:'search', value: '%'. $filterSearch . '%', type: SqlParam::STR);
                }
                $filterTenant = $filter->tenant();
                if (null !== $filterTenant) {
                    $query .= ' and "notification_smtp_outbound_config"."tenant" = :tenant';
                    $params[] = new SqlParam(name: 'tenant', value: $filterTenant->uid(), type: SqlParam::STR);
                }
                $filterGlobalOnly = $filter->globalOnly();
                if (null !== $filterGlobalOnly) {
                    $query .= ' and "tenant" is null ';
                }
                $filterTenants = $filter->tenants();
                if (null !== $filterTenants) {
                    $query .= ' and "notification_smtp_outbound_config"."tenant" in (:tenants)  ';
                    $params[] = new SqlParam(name: 'tenants', value: $filterTenants, type: SqlParam::STR);
                }
                $filterTenantTenantAccesible = $filter->tenantTenantAccesible();
                if (null !== $filterTenantTenantAccesible) {
                    $join .= ' LEFT JOIN "access_tenant" as "tenantTenantAccesibleTenant" ON "tenantTenantAccesibleTenant"."uid" = "notification_smtp_outbound_config"."tenant"';
                    $query .= ' and "tenantTenantAccesibleTenant"."uid" = :tenantTenantAccesible';
                    $params[] = new SqlParam(name: 'tenantTenantAccesible', value: $filterTenantTenantAccesible, type: SqlParam::STR);
                }
            }
            if ($sort) {
                $sortLimit = $sort->limit();
                if ($sortLimit) {
                    $limit = ' LIMIT ' . $this->db->escapeValue($sortLimit, SqlParam::INT);
                }
                $sortSinceUid = $sort->sinceUid();
                if (null !== $sortSinceUid) {
                    $query .= ' and  "uid" < :sinceUid';
                    $params[] = new SqlParam(name: 'sinceUid', value: $sortSinceUid, type: SqlParam::STR);
                }
                $order = ', "notification_smtp_outbound_config"."uid" desc';
            }
            return [
              'query' => 'SELECT '.($count ? ' count("notification_smtp_outbound_config".*) as count ' : '"notification_smtp_outbound_config".*').' FROM "notification_smtp_outbound_config"'
                . $join
                . ($query ? ' WHERE ' . substr($query, 4) : '')
                . ($order ? ' ORDER BY ' . substr($order, 2) : '') . $limit,
              'params' => $params];
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function mapper(array $row): SmtpOutboundConfig
    {
        $this->logDebug("Mapping from sql to entity for Smtp outbound config");
        $span = $this->startSpan("Mapping from sql to entity for Smtp outbound config");
        try {
            $uid = $row['uid'] ?? null;
            if (null === $uid) {
                throw ConstraintException::ofError('not-null', ['uid'], [null]);
            }
            $tenant = isset($row['tenant']) ? new TenantRef(uid: $row['tenant']) : null;
            $host = $row['host'] ?? null;
            if (null === $host) {
                throw ConstraintException::ofError('not-null', ['host'], [null]);
            }
            $port = isset($row['port']) ? !! $row['port'] : null;
            if (null === $port) {
                throw ConstraintException::ofError('not-null', ['port'], [null]);
            }
            $login = $row['login'] ?? null;
            if (null === $login) {
                throw ConstraintException::ofError('not-null', ['login'], [null]);
            }
            $rawPassword = $row['password'] ?? null;
            if (null === $rawPassword) {
                throw ConstraintException::ofError('not-null', ['password'], [null]);
            }
            $password = SmtpOutboundConfigPasswordVO::fromCypheredText($this->cypher, $rawPassword);
            $senderName = $row['sender_name'] ?? null;
            $senderEmail = $row['sender_email'] ?? null;
            if (null === $senderEmail) {
                throw ConstraintException::ofError('not-null', ['senderEmail'], [null]);
            }
            $timeout = $row['timeout'] ?? null;
            if (null === $timeout) {
                throw ConstraintException::ofError('not-null', ['timeout'], [null]);
            }
            $useTls = isset($row['use_tls']) ? !! $row['use_tls'] : null;
            if (null === $useTls) {
                throw ConstraintException::ofError('not-null', ['useTls'], [null]);
            }
            $maxRetries = $row['max_retries'] ?? null;
            if (null === $maxRetries) {
                throw ConstraintException::ofError('not-null', ['maxRetries'], [null]);
            }
            $retryDelay = $row['retry_delay'] ?? null;
            if (null === $retryDelay) {
                throw ConstraintException::ofError('not-null', ['retryDelay'], [null]);
            }
            $rateLimit = $row['rate_limit'] ?? null;
            if (null === $rateLimit) {
                throw ConstraintException::ofError('not-null', ['rateLimit'], [null]);
            }
            $version = $row['version'] ?? null;
            return new SmtpOutboundConfig(
                uid: $uid,
                tenant: $tenant,
                host: $host,
                port: $port,
                login: $login,
                password: $password,
                senderName: $senderName,
                senderEmail: $senderEmail,
                timeout: $timeout,
                useTls: $useTls,
                maxRetries: $maxRetries,
                retryDelay: $retryDelay,
                rateLimit: $rateLimit,
                version: $version,
            );
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Document\TemplateVersion\Infrastructure\Connector\Pdo;

use Throwable;
use Closure;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlParam;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlTemplate;
use Civi\Lughauth\Shared\Exception\NotFoundException;
use Civi\Lughauth\Shared\Exception\ConstraintException;
use Civi\Lughauth\Shared\Exception\OptimistLockException;
use Civi\Lughauth\Shared\Exception\NotEmptyChildsException;
use Civi\Lughauth\Features\Document\TemplateVersion\Domain\Gateway\TemplateVersionFilter;
use Civi\Lughauth\Features\Document\TemplateVersion\Domain\Gateway\TemplateVersionCursor;
use Civi\Lughauth\Features\Document\TemplateVersion\Domain\TemplateVersion;
use Civi\Lughauth\Features\Document\TemplateVersion\Domain\TemplateVersionRef;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;
use Civi\Lughauth\Features\Document\Template\Domain\TemplateRef;

class TemplateVersionPdoConnector
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly SqlTemplate $db,
    ) {
    }
    public function list(?TemplateVersionFilter $filter = null, ?TemplateVersionCursor $sort = null): array
    {
        $this->logDebug("List query for Template version");
        $span = $this->startSpan("List query for Template version");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function listForUpdate(?TemplateVersionFilter $filter = null, ?TemplateVersionCursor $sort = null): array
    {
        $this->logDebug("List query for update of Template version");
        $span = $this->startSpan("List query for update of Template version");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->queryForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function query(string $query, ?array $params = []): array
    {
        return $this->theQuery(false, $query, $params);
    }
    private function rawQuery(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(false, $query, $params);
    }
    private function queryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theQuery(true, $query, $params);
    }
    private function rawQueryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(true, $query, $params);
    }
    private function theQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make query for entities for Template version");
        $span = $this->startSpan("Make query for entities for Template version");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): TemplateVersion => $this->mapper($row)) : $this->db->query($query, $params, fn (array $row): TemplateVersion => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function theRawQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make raw query for Template version");
        $span = $this->startSpan("Make raw query for Template version");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): array => $row) : $this->db->query($query, $params, fn (array $row): array => $row);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieve(TemplateVersionFilter $filter): ?TemplateVersion
    {
        $this->logDebug("Retrieve query for Template version");
        $span = $this->startSpan("Retrieve query for Template version");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): TemplateVersion => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieveForUpdate(TemplateVersionFilter $filter): ?TemplateVersion
    {
        $this->logDebug("Retrieve query for update of Template version");
        $span = $this->startSpan("Retrieve query for update of Template version");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row): TemplateVersion => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function create(TemplateVersion $entity, ?Closure $verify = null): TemplateVersion
    {
        $this->logDebug("Execute insert sql query for Template version");
        $span = $this->startSpan("Execute insert sql query for Template version");
        try {
            $this->db->execute('INSERT INTO "document_template_version" ( "uid", "template", "subject", "content_html", "content_text", "version") VALUES ( :uid, :template, :subject, :contentHtml, :contentText, :version)', [
                 new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR),
                 new SqlParam(name: 'template', value: $entity->getTemplate()->uid(), type: SqlParam::STR),
                 new SqlParam(name: 'subject', value: $entity->getSubject(), type: SqlParam::STR),
                 new SqlParam(name: 'contentHtml', value: $entity->getContentHtml(), type: SqlParam::TEXT),
                 new SqlParam(name: 'contentText', value: $entity->getContentText(), type: SqlParam::TEXT),
                 new SqlParam(name: 'version', value: 0, type: SqlParam::INT)
            ]);
            $created = $entity->withVersion(0);
            if ($verify && !$verify($created)) {
                $this->db->execute('DELETE FROM "document_template_version" where "uid" = :uid', [
                  new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR)
                ]);
                throw new NotFoundException('Insertion of not visible values');
            }
            return $created;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function update(TemplateVersion $update): TemplateVersion
    {
        $this->logDebug("Execute update sql query for Template version");
        $span = $this->startSpan("Execute update sql query for Template version");
        try {
            $result = $this->db->execute('UPDATE "document_template_version" SET "template" = :template , "subject" = :subject , "content_html" = :contentHtml , "content_text" = :contentText , "version" = :version WHERE "uid" = :uid and "version" = :_lock_version', [
                 new SqlParam(name: 'uid', value: $update->uid(), type: SqlParam::STR),
                 new SqlParam(name: 'template', value: $update->getTemplate()->uid(), type: SqlParam::STR),
                 new SqlParam(name: 'subject', value: $update->getSubject(), type: SqlParam::STR),
                 new SqlParam(name: 'contentHtml', value: $update->getContentHtml(), type: SqlParam::TEXT),
                 new SqlParam(name: 'contentText', value: $update->getContentText(), type: SqlParam::TEXT),
                 new SqlParam(name: 'version', value: ($update->getVersion() ?? 0) + 1, type: SqlParam::INT),
                 new SqlParam(name: '_lock_version', value: $update->getVersion(), type: SqlParam::INT)
            ]);
            if (!$result && $this->db->exists('select "uid" from "document_template_version" where "uid" = :uid', ['uid' => $update->uid() ])) {
                throw new OptimistLockException($update->uid() ?? 'no-id', "version: " . ($update->getVersion() ?? 0));
            } elseif (!$result) {
                throw new NotFoundException($update->uid() ?? 'no-id');
            }
            return $update->withVersion(($update->getVersion() ?? 0) + 1);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function delete(TemplateVersionRef $ref): bool
    {
        $this->logDebug("Execute delete sql query for Template version");
        $span = $this->startSpan("Execute delete sql query for Template version");
        try {
            try {
                return $this->db->execute('DELETE FROM "document_template_version" where "uid" = :uid', [new SqlParam(name: 'uid', value: $ref->uid(), type:SqlParam::STR)]);
            } catch (NotEmptyChildsException $ex) {
                throw ConstraintException::ofError('not-empty', ['uid'], [$ref->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function exists(TemplateVersionFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for Template version");
        $span = $this->startSpan("Execute exists sql query for Template version");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function existsForUpdate(TemplateVersionFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for update of Template version");
        $span = $this->startSpan("Execute update sql query for update of Template version");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->existsForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function count(?TemplateVersionFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for Template version");
        $span = $this->startSpan("Execute count sql query for Template version");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): int => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function countForUpdate(?TemplateVersionFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for update of Template version");
        $span = $this->startSpan("Execute count sql query for update of Template version");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row) => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function filter(?TemplateVersionFilter $filter, ?TemplateVersionCursor $sort, bool $count): array
    {
        $this->logDebug("Build query filter of Template version");
        $span = $this->startSpan("Build query filter of Template version");
        try {
            $join = '';
            $query = '';
            $params = [];
            $order = '';
            $limit = '';
            if ($filter) {
                $filterUids = $filter->uids();
                if (null !== $filterUids && count($filterUids) > 1) {
                    $query .= ' and "document_template_version"."uid" in (:uids)';
                    $params[] = new SqlParam(name:'uids', value: $filterUids, type: SqlParam::STR);
                } elseif (null !== $filterUids) {
                    $query .= ' and "document_template_version"."uid" = :uid';
                    $params[] = new SqlParam(name:'uid', value: $filterUids[0], type: SqlParam::STR);
                }
                $filterSearch = $filter->search();
                if (null !== $filterSearch) {
                    $query .= ' and ( "document_template_version"."uid" like :search)';
                    $params[] = new SqlParam(name:'search', value: '%'. $filterSearch . '%', type: SqlParam::STR);
                }
                $filterTemplate = $filter->template();
                if (null !== $filterTemplate) {
                    $query .= ' and "document_template_version"."template" = :template ';
                    $params[] = new SqlParam(name: 'template', value: $filterTemplate->uid(), type: SqlParam::STR);
                }
                $filterTemplates = $filter->templates();
                if (null !== $filterTemplates) {
                    $query .= ' and "document_template_version"."template" in (:templates)  ';
                    $params[] = new SqlParam(name: 'templates', value: $filterTemplates, type: SqlParam::STR);
                }
                $filterTemplateTenantTenantAccesible = $filter->templateTenantTenantAccesible();
                if (null !== $filterTemplateTenantTenantAccesible) {
                    $join .= ' LEFT JOIN "document_template" as "templateTenantTenantAccesibleTemplate" ON "templateTenantTenantAccesibleTemplate"."uid" = "document_template_version"."template" LEFT JOIN "access_tenant" as "templateTenantTenantAccesibleTenant" ON "templateTenantTenantAccesibleTenant"."uid" = "templateTenantTenantAccesibleTemplate"."tenant"';
                    $query .= ' and "templateTenantTenantAccesibleTenant"."uid" = :templateTenantTenantAccesible';
                    $params[] = new SqlParam(name: 'templateTenantTenantAccesible', value: $filterTemplateTenantTenantAccesible, type: SqlParam::STR);
                }
            }
            if ($sort) {
                $sortLimit = $sort->limit();
                if ($sortLimit) {
                    $limit = ' LIMIT ' . $this->db->escapeValue($sortLimit, SqlParam::INT);
                }
                $sortSinceUid = $sort->sinceUid();
                if (null !== $sortSinceUid) {
                    $query .= ' and  "uid" < :sinceUid';
                    $params[] = new SqlParam(name: 'sinceUid', value: $sortSinceUid, type: SqlParam::STR);
                }
                $order = ', "document_template_version"."uid" desc';
            }
            return [
              'query' => 'SELECT '.($count ? ' count("document_template_version".*) as count ' : '"document_template_version".*').' FROM "document_template_version"'
                . $join
                . ($query ? ' WHERE ' . substr($query, 4) : '')
                . ($order ? ' ORDER BY ' . substr($order, 2) : '') . $limit,
              'params' => $params];
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function mapper(array $row): TemplateVersion
    {
        $this->logDebug("Mapping from sql to entity for Template version");
        $span = $this->startSpan("Mapping from sql to entity for Template version");
        try {
            $uid = $row['uid'] ?? null;
            if (null === $uid) {
                throw ConstraintException::ofError('not-null', ['uid'], [null]);
            }
            $rawTemplate = $row['template'] ?? null;
            if (null === $rawTemplate) {
                throw ConstraintException::ofError('not-null', ['template'], [null]);
            }
            $template = new TemplateRef(uid: $rawTemplate);
            $subject = $row['subject'] ?? null;
            $contentHtml = $row['content_html'] ?? null;
            if (null === $contentHtml) {
                throw ConstraintException::ofError('not-null', ['contentHtml'], [null]);
            }
            $contentText = $row['content_text'] ?? null;
            $version = $row['version'] ?? null;
            return new TemplateVersion(
                uid: $uid,
                template: $template,
                subject: $subject,
                contentHtml: $contentHtml,
                contentText: $contentText,
                version: $version,
            );
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

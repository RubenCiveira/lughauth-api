<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Document\TemplateVersion\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;
use Civi\Lughauth\Shared\Value\Validation\Rule\Length;

class TemplateVersionLocaleVO
{
    public static function empty(): TemplateVersionLocaleVO
    {
        return new TemplateVersionLocaleVO(null);
    }
    public static function from(TemplateVersionLocaleVO|string|null $value): TemplateVersionLocaleVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?TemplateVersionLocaleVO
    {
        if ($value instanceof TemplateVersionLocaleVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new TemplateVersionLocaleVO($value);
        } elseif (is_string($value)) {
            // If is a ValueObject, we need to append all the errors for the context
            $valid = true;
            foreach (self::rules() as $rule) {
                if ($fail = $rule->check($value)) {
                    $list->add(ConstraintFail::fromRuleFail('locale', $fail));
                    $valid = false;
                }
            }
            return $valid ? new TemplateVersionLocaleVO($value) : null;
        } else {
            $list->add(new ConstraintFail('wrong_type', ['locale'], [$value], ['string']));
            return null;
        }
    }
    public static function rules(): array
    {
        return [
          new Length(min: null, max: 250),
        ];
    }
    private static function fromUnsafe(mixed $value): TemplateVersionLocaleVO
    {
        if ($value instanceof TemplateVersionLocaleVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof TemplateVersionLocaleVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?string $locale
    ) {
    }
    public function value(): ?string
    {
        return $this->locale;
    }
    public function equals(?TemplateVersionLocaleVO $other): bool
    {
        return $this->value() == $other?->value();
    }
}

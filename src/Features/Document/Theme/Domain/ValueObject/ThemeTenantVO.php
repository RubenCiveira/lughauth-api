<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Document\Theme\Domain\ValueObject;

use Civi\Lughauth\Features\Access\Tenant\Domain\TenantRef;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;

class ThemeTenantVO
{
    public static function empty(): ThemeTenantVO
    {
        return new ThemeTenantVO(null);
    }
    public static function from(ThemeTenantVO|TenantRef|null $value): ThemeTenantVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?ThemeTenantVO
    {
        if ($value instanceof ThemeTenantVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new ThemeTenantVO($value);
        } elseif (is_a($value, TenantRef::class)) {
            return new ThemeTenantVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['tenant'], [$value], ['TenantRef']));
            return null;
        }
    }
    private static function fromUnsafe(mixed $value): ThemeTenantVO
    {
        if ($value instanceof ThemeTenantVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof ThemeTenantVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?TenantRef $tenant
    ) {
    }
    public function value(): ?TenantRef
    {
        return $this->tenant;
    }
    public function equals(?ThemeTenantVO $other): bool
    {
        return $this->value()?->uid() == $other?->value()?->uid();
    }
}

<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Document\ThemeVersion\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;
use Civi\Lughauth\Features\Document\ThemeVersion\Domain\ThemeVersionChannelOptions;

class ThemeVersionChannelVO
{
    public static function fromString(string $value): ThemeVersionChannelVO
    {
        $errorsList = new ConstraintFailList();
        $candidate = self::tryFrom($value, $errorsList);
        if ($errorsList->hasErrors()) {
            throw $errorsList->asConstraintException();
        }
        \assert($candidate instanceof ThemeVersionChannelVO);
        return $candidate;
    }
    public static function from(ThemeVersionChannelVO|ThemeVersionChannelOptions $value): ThemeVersionChannelVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?ThemeVersionChannelVO
    {
        if ($value instanceof ThemeVersionChannelVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (is_string($value)) {
            if ($typed = self::readFromString($value)) {
                return self::tryFrom($typed, $list);
            } else {
                $list->add(new ConstraintFail('wrong_format', ['channel'], [$value], ['ThemeVersionChannelOptions']));
                return null;
            }
        } elseif (is_a($value, ThemeVersionChannelOptions::class)) {
            return new ThemeVersionChannelVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['channel'], [$value], ['ThemeVersionChannelOptions']));
            return null;
        }
    }
    private static function readFromString(string $enumString): ?ThemeVersionChannelOptions
    {
        if (in_array($enumString, ['MAIL', 'mail', 'mAIL'])) {
            return ThemeVersionChannelOptions::MAIL;
        }
        if (in_array($enumString, ['HTML', 'html', 'hTML'])) {
            return ThemeVersionChannelOptions::HTML;
        }
        return null;
    }
    private static function fromUnsafe(mixed $value): ThemeVersionChannelVO
    {
        if ($value instanceof ThemeVersionChannelVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof ThemeVersionChannelVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ThemeVersionChannelOptions $channel
    ) {
    }
    public function value(): ThemeVersionChannelOptions
    {
        return $this->channel;
    }
    public function equals(?ThemeVersionChannelVO $other): bool
    {
        return $this->value() == $other?->value();
    }
}

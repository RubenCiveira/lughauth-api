<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Document\Snippet\Infrastructure\Driver\Rest;

use Psr\Http\Message\ResponseInterface;
use Psr\Http\Message\ServerRequestInterface;
use Throwable;
use OpenApi\Attributes as OA;
use Civi\Lughauth\Features\Document\Snippet\Domain\Gateway\SnippetFilter;
use Civi\Lughauth\Features\Document\Snippet\Domain\Gateway\SnippetCursor;
use Civi\Lughauth\Features\Document\Snippet\Domain\SnippetAttributes;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;
use Civi\Lughauth\Features\Document\Snippet\Application\Usecase\List\SnippetListUsecase;
use Civi\Lughauth\Features\Access\Tenant\Domain\TenantRef;

class SnippetListController
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly SnippetListUsecase $listUsecase,
    ) {
    }
    #[OA\Get(
        path: "/api/document/snippet",
        tags: ["Snippet"],
        description:"Create",
        responses: [
        new OA\Response(response: 201, description: "Ok", content: new OA\JsonContent(type: "object", ref:"#/components/schemas/SnippetApiListDTO")),
        new OA\Response(response: 422, description: "Constraint error"),
    ]
    )
    ]
    public function list(ServerRequestInterface $request, ResponseInterface $response, array $args): ResponseInterface
    {
        $this->logDebug("List Snippet");
        $span = $this->startSpan("List Snippet");
        try {
            $params = $request->getQueryParams();
            $filter = new SnippetFilter(
                uids: isset($params['uid']) ? [$params['uid']] : (isset($params['uids']) ? explode(',', $params['uids']) : null),
                search: $params['search'] ?? null,
                global: isset($params['global']) ? filter_var($params['global'], FILTER_VALIDATE_BOOLEAN, FILTER_NULL_ON_FAILURE) : null,
                code: $params['code'] ?? null,
                tenant: isset($params['tenant']) ? new TenantRef($params['tenant']) : null,
                tenants: isset($params['tenants']) ? explode(",", $params['tenants']) : null,
            );
            $cursor = new SnippetCursor(
                limit: (int)($params['limit'] ?? 100),
                sinceUid: $params['since-uid'] ?? null,
                sinceCode: $params['since-code'] ?? null,
                order: isset($params['order']) ? $this->extractOrder(explode(',', $params['order'])) : null,
            );
            $result = $this->listUsecase->list($filter, $cursor);
            $value = ['items' => array_map(fn (SnippetAttributes $item): SnippetApiDTO => $this->mapSnippet($request, $item), $result->values())];
            $nextLink = $this->nextLink($result->cursor(), $params);
            if (null !== $nextLink) {
                $value['next'] = "?{$nextLink}";
            }
            $encoded = json_encode($value);
            $response->getBody()->write($encoded === false ? '' : $encoded);
            return $response->withStatus(200)
              ->withHeader('Content-Type', 'application/json');
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function extractOrder(array $order): array
    {
        $this->logDebug("Extract order for Snippet");
        $span = $this->startSpan("Extract order for Snippet");
        try {
            $mapped = [];
            foreach ($order as $ord) {
                if ($ord === 'code-asc') {
                    $mapped[] = 'codeAsc';
                }
                if ($ord === 'code-desc') {
                    $mapped[] = 'codeDesc';
                }
            }
            return $mapped;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function nextLink(?SnippetCursor $value, array $params): ?string
    {
        $this->logDebug("Check generic allow for Snippet");
        $span = $this->startSpan("Generic allow for Snippet");
        try {
            if (!$value) {
                return null;
            }
            $link = [];
            if (isset($params['limit'])) {
                $link['limit'] = 'limit=' . $params['limit'];
            }
            $sinceUid = $value->sinceUid();
            if (null !== $sinceUid) {
                $link['since-uid'] = 'since-uid=' . urlencode($sinceUid);
            }
            $sinceCode = $value->sinceCode();
            if (null !== $sinceCode &&  false !== strpos($params['order'] ?? '', 'CODE-ASC')) {
                $link['since-code'] = 'since-code=' . urlencode($sinceCode);
            }
            $sinceCode = $value->sinceCode();
            if (null !== $sinceCode &&  false !== strpos($params['order'] ?? '', 'CODE-DESC')) {
                $link['since-code'] = 'since-code=' . urlencode($sinceCode);
            }
            return implode('&', $link);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }

    private function mapSnippet(ServerRequestInterface $request, SnippetAttributes $value): SnippetApiDTO
    {
        $this->logDebug("Map entity to output dto for Snippet");
        $span = $this->startSpan("Map entity to output dto for Snippet");
        try {
            $tenant = $value->getTenant();
            $dto = new SnippetApiDTO();
            $dto->uid = $value->getUid();
            $dto->code = $value->getCode();
            $dto->tenant = $tenant ? ['$ref' => $tenant->uid()] : null;
            $dto->enabled = $value->isEnabled();
            $dto->version = $value->getVersion();
            return $dto;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

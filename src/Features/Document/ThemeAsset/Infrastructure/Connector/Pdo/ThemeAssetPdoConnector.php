<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Document\ThemeAsset\Infrastructure\Connector\Pdo;

use Throwable;
use Closure;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlParam;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlTemplate;
use Civi\Lughauth\Shared\Exception\NotFoundException;
use Civi\Lughauth\Shared\Exception\NotUniqueException;
use Civi\Lughauth\Shared\Exception\ConstraintException;
use Civi\Lughauth\Shared\Exception\OptimistLockException;
use Civi\Lughauth\Shared\Exception\NotEmptyChildsException;
use Civi\Lughauth\Features\Document\ThemeAsset\Domain\Gateway\ThemeAssetFilter;
use Civi\Lughauth\Features\Document\ThemeAsset\Domain\Gateway\ThemeAssetCursor;
use Civi\Lughauth\Features\Document\ThemeAsset\Domain\ThemeAsset;
use Civi\Lughauth\Features\Document\ThemeAsset\Domain\ThemeAssetRef;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;
use Civi\Lughauth\Features\Document\Theme\Domain\ThemeRef;
use Civi\Lughauth\Features\Document\ThemeAsset\Domain\ValueObject\ThemeAssetContentVO;

class ThemeAssetPdoConnector
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly SqlTemplate $db,
    ) {
    }
    public function list(?ThemeAssetFilter $filter = null, ?ThemeAssetCursor $sort = null): array
    {
        $this->logDebug("List query for Theme asset");
        $span = $this->startSpan("List query for Theme asset");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function listForUpdate(?ThemeAssetFilter $filter = null, ?ThemeAssetCursor $sort = null): array
    {
        $this->logDebug("List query for update of Theme asset");
        $span = $this->startSpan("List query for update of Theme asset");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->queryForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function query(string $query, ?array $params = []): array
    {
        return $this->theQuery(false, $query, $params);
    }
    private function rawQuery(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(false, $query, $params);
    }
    private function queryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theQuery(true, $query, $params);
    }
    private function rawQueryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(true, $query, $params);
    }
    private function theQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make query for entities for Theme asset");
        $span = $this->startSpan("Make query for entities for Theme asset");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): ThemeAsset => $this->mapper($row)) : $this->db->query($query, $params, fn (array $row): ThemeAsset => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function theRawQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make raw query for Theme asset");
        $span = $this->startSpan("Make raw query for Theme asset");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): array => $row) : $this->db->query($query, $params, fn (array $row): array => $row);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieve(ThemeAssetFilter $filter): ?ThemeAsset
    {
        $this->logDebug("Retrieve query for Theme asset");
        $span = $this->startSpan("Retrieve query for Theme asset");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): ThemeAsset => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieveForUpdate(ThemeAssetFilter $filter): ?ThemeAsset
    {
        $this->logDebug("Retrieve query for update of Theme asset");
        $span = $this->startSpan("Retrieve query for update of Theme asset");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row): ThemeAsset => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function create(ThemeAsset $entity, ?Closure $verify = null): ThemeAsset
    {
        $this->logDebug("Execute insert sql query for Theme asset");
        $span = $this->startSpan("Execute insert sql query for Theme asset");
        try {
            try {
                $this->db->execute('INSERT INTO "document_theme_asset" ( "uid", "theme", "code", "type", "content", "enabled", "version") VALUES ( :uid, :theme, :code, :type, :content, :enabled, :version)', [
                     new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'theme', value: $entity->getTheme()->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'code', value: $entity->getCode(), type: SqlParam::STR),
                     new SqlParam(name: 'type', value: $entity->getType(), type: SqlParam::STR),
                     new SqlParam(name: 'content', value: $entity->getContent(), type: SqlParam::STR),
                     new SqlParam(name: 'enabled', value: $entity->isEnabled(), type: SqlParam::BOOL),
                     new SqlParam(name: 'version', value: 0, type: SqlParam::INT)
                ]);
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($entity, true);
                throw $ex;
            }
            $created = $entity->withVersion(0);
            if ($verify && !$verify($created)) {
                $this->db->execute('DELETE FROM "document_theme_asset" where "uid" = :uid', [
                  new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR)
                ]);
                throw new NotFoundException('Insertion of not visible values');
            }
            return $created;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function update(ThemeAsset $update): ThemeAsset
    {
        $this->logDebug("Execute update sql query for Theme asset");
        $span = $this->startSpan("Execute update sql query for Theme asset");
        try {
            try {
                $result = $this->db->execute('UPDATE "document_theme_asset" SET "theme" = :theme , "code" = :code , "type" = :type , "content" = :content , "enabled" = :enabled , "version" = :version WHERE "uid" = :uid and "version" = :_lock_version', [
                     new SqlParam(name: 'uid', value: $update->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'theme', value: $update->getTheme()->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'code', value: $update->getCode(), type: SqlParam::STR),
                     new SqlParam(name: 'type', value: $update->getType(), type: SqlParam::STR),
                     new SqlParam(name: 'content', value: $update->getContent(), type: SqlParam::STR),
                     new SqlParam(name: 'enabled', value: $update->isEnabled(), type: SqlParam::BOOL),
                     new SqlParam(name: 'version', value: ($update->getVersion() ?? 0) + 1, type: SqlParam::INT),
                     new SqlParam(name: '_lock_version', value: $update->getVersion(), type: SqlParam::INT)
                ]);
                if (!$result && $this->db->exists('select "uid" from "document_theme_asset" where "uid" = :uid', ['uid' => $update->uid() ])) {
                    throw new OptimistLockException($update->uid() ?? 'no-id', "version: " . ($update->getVersion() ?? 0));
                } elseif (!$result) {
                    throw new NotFoundException($update->uid() ?? 'no-id');
                }
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($update, false);
                throw $ex;
            }
            return $update->withVersion(($update->getVersion() ?? 0) + 1);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function delete(ThemeAssetRef $ref): bool
    {
        $this->logDebug("Execute delete sql query for Theme asset");
        $span = $this->startSpan("Execute delete sql query for Theme asset");
        try {
            try {
                return $this->db->execute('DELETE FROM "document_theme_asset" where "uid" = :uid', [new SqlParam(name: 'uid', value: $ref->uid(), type:SqlParam::STR)]);
            } catch (NotEmptyChildsException $ex) {
                throw ConstraintException::ofError('not-empty', ['uid'], [$ref->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function exists(ThemeAssetFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for Theme asset");
        $span = $this->startSpan("Execute exists sql query for Theme asset");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function existsForUpdate(ThemeAssetFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for update of Theme asset");
        $span = $this->startSpan("Execute update sql query for update of Theme asset");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->existsForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function count(?ThemeAssetFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for Theme asset");
        $span = $this->startSpan("Execute count sql query for Theme asset");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): int => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function countForUpdate(?ThemeAssetFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for update of Theme asset");
        $span = $this->startSpan("Execute count sql query for update of Theme asset");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row) => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function checkDuplicates(ThemeAsset $entity, bool $creation): void
    {
        $this->logDebug("Query to check duplicates for Theme asset");
        $span = $this->startSpan("Query to check duplicates for Theme asset");
        try {
            $values = ['code' => $entity->getCode(), 'theme' => $entity->getTheme()->uid(), 'uid' => $entity->uid()];
            if ($this->db->exists('SELECT  "code", "theme" from "document_theme_asset" where "code" = :code and "theme" = :theme and "uid" != :uid', $values)) {
                throw ConstraintException::ofError('not-unique', ['code', 'theme'], [$entity->getCode(), $entity->getTheme()->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function filter(?ThemeAssetFilter $filter, ?ThemeAssetCursor $sort, bool $count): array
    {
        $this->logDebug("Build query filter of Theme asset");
        $span = $this->startSpan("Build query filter of Theme asset");
        try {
            $join = '';
            $query = '';
            $params = [];
            $order = '';
            $limit = '';
            if ($filter) {
                $filterUids = $filter->uids();
                if (null !== $filterUids && count($filterUids) > 1) {
                    $query .= ' and "document_theme_asset"."uid" in (:uids)';
                    $params[] = new SqlParam(name:'uids', value: $filterUids, type: SqlParam::STR);
                } elseif (null !== $filterUids) {
                    $query .= ' and "document_theme_asset"."uid" = :uid';
                    $params[] = new SqlParam(name:'uid', value: $filterUids[0], type: SqlParam::STR);
                }
                $filterSearch = $filter->search();
                if (null !== $filterSearch) {
                    $query .= ' and ( "document_theme_asset"."code" like :search)';
                    $params[] = new SqlParam(name:'search', value: '%'. $filterSearch . '%', type: SqlParam::STR);
                }
                if ($filter->isCodeAndThemeAssigned()) {
                    $filterCodeAndTheme = $filter->codeAndTheme();
                    $codeAndThemeParts = [];
                    $codeAndThemeParts[] = ' "document_theme_asset"."code" = :codeThemeCode';
                    $params[] = new SqlParam(name: 'codeThemeCode', value: $filterCodeAndTheme['code'], type: SqlParam::STR);
                    $codeAndThemeParts[] = ' "document_theme_asset"."theme" = :codeThemeTheme';
                    $params[] = new SqlParam(name: 'codeThemeTheme', value: $filterCodeAndTheme['theme']->uid(), type: SqlParam::STR);
                    $query .= ' and ( ' . implode(' and ', $codeAndThemeParts) . ')';
                }
                $filterCode = $filter->code();
                if (null !== $filterCode) {
                    $query .= ' and "document_theme_asset"."code" = :code ';
                    $params[] = new SqlParam(name: 'code', value: $filterCode, type: SqlParam::STR);
                }
                $filterTheme = $filter->theme();
                if (null !== $filterTheme) {
                    $query .= ' and "document_theme_asset"."theme" = :theme ';
                    $params[] = new SqlParam(name: 'theme', value: $filterTheme->uid(), type: SqlParam::STR);
                }
                $filterThemes = $filter->themes();
                if (null !== $filterThemes) {
                    $query .= ' and "document_theme_asset"."theme" in (:themes)  ';
                    $params[] = new SqlParam(name: 'themes', value: $filterThemes, type: SqlParam::STR);
                }
                $filterThemeTenantAccesible = $filter->themeTenantAccesible();
                if (null !== $filterThemeTenantAccesible) {
                    $join .= ' LEFT JOIN "document_theme" as "themeTenantAccesibleTheme" ON "themeTenantAccesibleTheme"."uid" = "document_theme_asset"."theme" LEFT JOIN "access_tenant" as "themeTenantAccesibleTenant" ON "themeTenantAccesibleTenant"."uid" = "themeTenantAccesibleTheme"."tenant"';
                    $query .= ' and "themeTenantAccesibleTenant"."uid" = :themeTenantAccesible';
                    $params[] = new SqlParam(name: 'themeTenantAccesible', value: $filterThemeTenantAccesible, type: SqlParam::STR);
                }
            }
            if ($sort) {
                $sortLimit = $sort->limit();
                if ($sortLimit) {
                    $limit = ' LIMIT ' . $this->db->escapeValue($sortLimit, SqlParam::INT);
                }
                $sortOrder = $sort->order();
                if (null !== $sortOrder) {
                    $equals = '';
                    foreach ($sortOrder as $ord) {
                        if ($ord === 'codeAsc') {
                            $sortSinceCode = $sort->sinceCode();
                            if (null !== $sortSinceCode) {
                                $query .= " and " . ($equals ? substr($equals, 4) . ' and ' : '') . ' "theme-asset"."code" > :sinceCode';
                                $equals .= ' and "theme-asset"."code" = :sinceCode';
                                $params[] = new SqlParam(name: 'sinceCode', value: $sortSinceCode, type: SqlParam::STR);
                            }
                            $order .= ', "document_theme_asset"."code" asc';
                        }
                        if ($ord === 'codeDesc') {
                            $sortSinceCode = $sort->sinceCode();
                            if (null !== $sortSinceCode) {
                                $query .= " and " . ($equals ? substr($equals, 4) . ' and ' : '') . ' "theme-asset"."code" < :sinceCode';
                                $equals .= ' and "theme-asset"."code" = :sinceCode';
                                $params[] = new SqlParam(name: 'sinceCode', value: $sortSinceCode, type: SqlParam::STR);
                            }
                            $order .= ', "document_theme_asset"."code" desc';
                        }
                    }
                } else {
                    $sortSinceUid = $sort->sinceUid();
                    if (null !== $sortSinceUid) {
                        $query .= ' and  "document_theme_asset"."uid" < :sinceUid';
                        $params[] = new SqlParam(name: 'sinceUid', value: $sortSinceUid, type: SqlParam::STR);
                    }
                    $order = ', "document_theme_asset"."uid" desc';
                }
            }
            return [
              'query' => 'SELECT '.($count ? ' count("document_theme_asset".*) as count ' : '"document_theme_asset".*').' FROM "document_theme_asset"'
                . $join
                . ($query ? ' WHERE ' . substr($query, 4) : '')
                . ($order ? ' ORDER BY ' . substr($order, 2) : '') . $limit,
              'params' => $params];
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function mapper(array $row): ThemeAsset
    {
        $this->logDebug("Mapping from sql to entity for Theme asset");
        $span = $this->startSpan("Mapping from sql to entity for Theme asset");
        try {
            $uid = $row['uid'] ?? null;
            if (null === $uid) {
                throw ConstraintException::ofError('not-null', ['uid'], [null]);
            }
            $rawTheme = $row['theme'] ?? null;
            if (null === $rawTheme) {
                throw ConstraintException::ofError('not-null', ['theme'], [null]);
            }
            $theme = new ThemeRef(uid: $rawTheme);
            $code = $row['code'] ?? null;
            if (null === $code) {
                throw ConstraintException::ofError('not-null', ['code'], [null]);
            }
            $type = $row['type'] ?? null;
            if (null === $type) {
                throw ConstraintException::ofError('not-null', ['type'], [null]);
            }
            $rawContent = $row['content'] ?? null;
            if (null === $rawContent) {
                throw ConstraintException::ofError('not-null', ['content'], [null]);
            }
            $content = ThemeAssetContentVO::fromStored($rawContent);
            $enabled = isset($row['enabled']) ? !! $row['enabled'] : null;
            $version = $row['version'] ?? null;
            return new ThemeAsset(
                uid: $uid,
                theme: $theme,
                code: $code,
                type: $type,
                content: $content,
                enabled: $enabled,
                version: $version,
            );
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\ConsentPurpose\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;
use Civi\Lughauth\Features\Access\ConsentPurpose\Domain\ConsentPurposeKeyOptions;

class ConsentPurposeKeyVO
{
    public static function fromString(string $value): ConsentPurposeKeyVO
    {
        $errorsList = new ConstraintFailList();
        $candidate = self::tryFrom($value, $errorsList);
        if ($errorsList->hasErrors()) {
            throw $errorsList->asConstraintException();
        }
        \assert($candidate instanceof ConsentPurposeKeyVO);
        return $candidate;
    }
    public static function from(ConsentPurposeKeyVO|ConsentPurposeKeyOptions $value): ConsentPurposeKeyVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?ConsentPurposeKeyVO
    {
        if ($value instanceof ConsentPurposeKeyVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (is_string($value)) {
            if ($typed = self::readFromString($value)) {
                return self::tryFrom($typed, $list);
            } else {
                $list->add(new ConstraintFail('wrong_format', ['key'], [$value], ['ConsentPurposeKeyOptions']));
                return null;
            }
        } elseif (is_a($value, ConsentPurposeKeyOptions::class)) {
            return new ConsentPurposeKeyVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['key'], [$value], ['ConsentPurposeKeyOptions']));
            return null;
        }
    }
    private static function readFromString(string $enumString): ?ConsentPurposeKeyOptions
    {
        if (in_array($enumString, ['marketing', 'MARKETING', 'mARKETING'])) {
            return ConsentPurposeKeyOptions::MARKETING;
        }
        if (in_array($enumString, ['analytics', 'ANALYTICS', 'aNALYTICS'])) {
            return ConsentPurposeKeyOptions::ANALYTICS;
        }
        if (in_array($enumString, ['THIRDPARTY', 'tHIRDPARTY', 'third_party', 'THIRD_PARTY', 'third-party'])) {
            return ConsentPurposeKeyOptions::THIRD_PARTY;
        }
        if (in_array($enumString, ['personalization', 'PERSONALIZATION', 'pERSONALIZATION'])) {
            return ConsentPurposeKeyOptions::PERSONALIZATION;
        }
        return null;
    }
    private static function fromUnsafe(mixed $value): ConsentPurposeKeyVO
    {
        if ($value instanceof ConsentPurposeKeyVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof ConsentPurposeKeyVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ConsentPurposeKeyOptions $key
    ) {
    }
    public function value(): ConsentPurposeKeyOptions
    {
        return $this->key;
    }
    public function equals(?ConsentPurposeKeyVO $other): bool
    {
        return $this->value() == $other?->value();
    }
}

<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\UserGrantedClientScopes\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;
use Civi\Lughauth\Shared\Value\Validation\Rule\Length;

class UserGrantedClientScopesScopeVO
{
    public static function empty(): UserGrantedClientScopesScopeVO
    {
        return new UserGrantedClientScopesScopeVO(null);
    }
    public static function from(UserGrantedClientScopesScopeVO|string|null $value): UserGrantedClientScopesScopeVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?UserGrantedClientScopesScopeVO
    {
        if ($value instanceof UserGrantedClientScopesScopeVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new UserGrantedClientScopesScopeVO($value);
        } elseif (is_string($value)) {
            // If is a ValueObject, we need to append all the errors for the context
            $valid = true;
            foreach (self::rules() as $rule) {
                if ($fail = $rule->check($value)) {
                    $list->add(ConstraintFail::fromRuleFail('scope', $fail));
                    $valid = false;
                }
            }
            return $valid ? new UserGrantedClientScopesScopeVO($value) : null;
        } else {
            $list->add(new ConstraintFail('wrong_type', ['scope'], [$value], ['string']));
            return null;
        }
    }
    public static function rules(): array
    {
        return [
          new Length(min: null, max: 250),
        ];
    }
    private static function fromUnsafe(mixed $value): UserGrantedClientScopesScopeVO
    {
        if ($value instanceof UserGrantedClientScopesScopeVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof UserGrantedClientScopesScopeVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?string $scope
    ) {
    }
    public function value(): ?string
    {
        return $this->scope;
    }
    public function equals(?UserGrantedClientScopesScopeVO $other): bool
    {
        return $this->value() == $other?->value();
    }
}

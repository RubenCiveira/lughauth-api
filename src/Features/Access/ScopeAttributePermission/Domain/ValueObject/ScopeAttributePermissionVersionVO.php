<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\ScopeAttributePermission\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;

class ScopeAttributePermissionVersionVO
{
    public static function empty(): ScopeAttributePermissionVersionVO
    {
        return new ScopeAttributePermissionVersionVO(null);
    }
    public static function from(ScopeAttributePermissionVersionVO|int|null $value): ScopeAttributePermissionVersionVO
    {
        if (is_a($value, ScopeAttributePermissionVersionVO::class)) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            return self::tryFrom($value, $errorsList);
        }
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?ScopeAttributePermissionVersionVO
    {
        if (is_a($value, ScopeAttributePermissionVersionVO::class)) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new ScopeAttributePermissionVersionVO($value);
        } elseif (is_integer($value)) {
            return new ScopeAttributePermissionVersionVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['version'], [$value], ['int']));
            return null;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?int $version
    ) {
    }
    public function value(): ?int
    {
        return $this->version;
    }
}

<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\UserProfile\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;
use Civi\Lughauth\Shared\Value\Validation\Rule\Length;

class UserProfileMiddleNameVO
{
    public static function empty(): UserProfileMiddleNameVO
    {
        return new UserProfileMiddleNameVO(null);
    }
    public static function from(UserProfileMiddleNameVO|string|null $value): UserProfileMiddleNameVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?UserProfileMiddleNameVO
    {
        if ($value instanceof UserProfileMiddleNameVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new UserProfileMiddleNameVO($value);
        } elseif (is_string($value)) {
            // If is a ValueObject, we need to append all the errors for the context
            $valid = true;
            foreach (self::rules() as $rule) {
                if ($fail = $rule->check($value)) {
                    $list->add(ConstraintFail::fromRuleFail('middleName', $fail));
                    $valid = false;
                }
            }
            return $valid ? new UserProfileMiddleNameVO($value) : null;
        } else {
            $list->add(new ConstraintFail('wrong_type', ['middleName'], [$value], ['string']));
            return null;
        }
    }
    public static function rules(): array
    {
        return [
          new Length(min: null, max: 250),
        ];
    }
    private static function fromUnsafe(mixed $value): UserProfileMiddleNameVO
    {
        if ($value instanceof UserProfileMiddleNameVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof UserProfileMiddleNameVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?string $middleName
    ) {
    }
    public function value(): ?string
    {
        return $this->middleName;
    }
    public function equals(?UserProfileMiddleNameVO $other): bool
    {
        return $this->value() == $other?->value();
    }
}

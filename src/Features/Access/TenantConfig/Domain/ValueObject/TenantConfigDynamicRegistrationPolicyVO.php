<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\TenantConfig\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;
use Civi\Lughauth\Features\Access\TenantConfig\Domain\TenantConfigDynamicRegistrationPolicyOptions;

class TenantConfigDynamicRegistrationPolicyVO
{
    public static function fromString(string $value): TenantConfigDynamicRegistrationPolicyVO
    {
        $errorsList = new ConstraintFailList();
        $candidate = self::tryFrom($value, $errorsList);
        if ($errorsList->hasErrors()) {
            throw $errorsList->asConstraintException();
        }
        \assert($candidate instanceof TenantConfigDynamicRegistrationPolicyVO);
        return $candidate;
    }
    public static function empty(): TenantConfigDynamicRegistrationPolicyVO
    {
        return new TenantConfigDynamicRegistrationPolicyVO(null);
    }
    public static function from(TenantConfigDynamicRegistrationPolicyVO|TenantConfigDynamicRegistrationPolicyOptions|null $value): TenantConfigDynamicRegistrationPolicyVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?TenantConfigDynamicRegistrationPolicyVO
    {
        if ($value instanceof TenantConfigDynamicRegistrationPolicyVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new TenantConfigDynamicRegistrationPolicyVO($value);
        } elseif (is_string($value)) {
            if ($typed = self::readFromString($value)) {
                return self::tryFrom($typed, $list);
            } else {
                $list->add(new ConstraintFail('wrong_format', ['dynamicRegistrationPolicy'], [$value], ['TenantConfigDynamicRegistrationPolicyOptions']));
                return null;
            }
        } elseif (is_a($value, TenantConfigDynamicRegistrationPolicyOptions::class)) {
            return new TenantConfigDynamicRegistrationPolicyVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['dynamicRegistrationPolicy'], [$value], ['TenantConfigDynamicRegistrationPolicyOptions']));
            return null;
        }
    }
    private static function readFromString(string $enumString): ?TenantConfigDynamicRegistrationPolicyOptions
    {
        if (in_array($enumString, ['oPEN', 'open', 'OPEN'])) {
            return TenantConfigDynamicRegistrationPolicyOptions::OPEN;
        }
        if (in_array($enumString, ['token-required', 'TOKEN_REQUIRED', 'TOKENREQUIRED', 'tOKENREQUIRED', 'token_required'])) {
            return TenantConfigDynamicRegistrationPolicyOptions::TOKEN_REQUIRED;
        }
        if (in_array($enumString, ['DISABLED', 'disabled', 'dISABLED'])) {
            return TenantConfigDynamicRegistrationPolicyOptions::DISABLED;
        }
        return null;
    }
    private static function fromUnsafe(mixed $value): TenantConfigDynamicRegistrationPolicyVO
    {
        if ($value instanceof TenantConfigDynamicRegistrationPolicyVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof TenantConfigDynamicRegistrationPolicyVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?TenantConfigDynamicRegistrationPolicyOptions $dynamicRegistrationPolicy
    ) {
    }
    public function value(): ?TenantConfigDynamicRegistrationPolicyOptions
    {
        return $this->dynamicRegistrationPolicy;
    }
    public function equals(?TenantConfigDynamicRegistrationPolicyVO $other): bool
    {
        return $this->value() == $other?->value();
    }
}

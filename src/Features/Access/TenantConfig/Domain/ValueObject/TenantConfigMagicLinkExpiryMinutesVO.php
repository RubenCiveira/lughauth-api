<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\TenantConfig\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;

class TenantConfigMagicLinkExpiryMinutesVO
{
    public static function from(TenantConfigMagicLinkExpiryMinutesVO|int $value): TenantConfigMagicLinkExpiryMinutesVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?TenantConfigMagicLinkExpiryMinutesVO
    {
        if ($value instanceof TenantConfigMagicLinkExpiryMinutesVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (is_integer($value)) {
            return new TenantConfigMagicLinkExpiryMinutesVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['magicLinkExpiryMinutes'], [$value], ['int']));
            return null;
        }
    }
    private static function fromUnsafe(mixed $value): TenantConfigMagicLinkExpiryMinutesVO
    {
        if ($value instanceof TenantConfigMagicLinkExpiryMinutesVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof TenantConfigMagicLinkExpiryMinutesVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly int $magicLinkExpiryMinutes
    ) {
    }
    public function value(): int
    {
        return $this->magicLinkExpiryMinutes;
    }
    public function equals(?TenantConfigMagicLinkExpiryMinutesVO $other): bool
    {
        return $this->value() == $other?->value();
    }
}

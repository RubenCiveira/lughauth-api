<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\UserInvitation\Domain\Gateway;

use Override;
use ArrayIterator;
use Civi\Lughauth\Features\Access\UserInvitation\Domain\UserInvitation;

/**
 * @template-extends ArrayIterator<int, UserInvitation>
 */
class UserInvitationSlide extends \ArrayIterator
{
    private readonly ?UserInvitation $last;
    private readonly UserInvitationCursor $nextCursor;
    public function __construct(
        private readonly ?\Closure $next,
        private readonly ?UserInvitationCursor $cursor,
        private readonly array $values
    ) {
        parent::__construct($values);
        $this->last = $values ? end($values) : null;
        $cursor = $this->cursor;
        $last = $this->last;
        $this->nextCursor = new UserInvitationCursor(
            limit: null === $cursor ? 1000 : $cursor->limit(),
            sinceUid: null == $last ? null : $last->uid(),
            sinceCreatedAt: null === $last ? null : $last->getCreatedAt(),
        );
    }
    public function cursor(): ?UserInvitationCursor
    {
        return $this->cursor;
    }
    public function nextCursor(): UserInvitationCursor
    {
        return $this->nextCursor;
    }
    public function values(): array
    {
        return $this->values;
    }
    #[Override]
    public function current(): ?UserInvitation
    {
        return parent::current();
    }
    public function map(\Closure $fn): UserInvitationSlide
    {
        return new UserInvitationSlide($this->next, $this->cursor, array_map($fn, $this->values));
    }
    public function slide(\Closure $fn): UserInvitationSlide
    {
        $filtered = array_filter($this->values, $fn);
        $collected = $filtered;
        $last = $this->last;
        $next = $this->next;
        $cursor = $this->cursor;
        if (null !== $cursor && count($collected) == $cursor->limit()) {
            while (count($collected) < count($this->values) && $next !== null) {
                if ($last === null) {
                    break;
                }
                $nextSlide = $next($this, $cursor->next($last, $cursor->limit()));
                if (count($nextSlide)) {
                    $nextFiltered = array_filter($nextSlide->values, $fn);
                    $collected = array_merge($collected, $nextFiltered);
                    $cursor = $nextSlide->cursor;
                    $next = $nextSlide->next;
                    $last = $nextSlide->last;
                } else {
                    break;
                }
            }
            return new UserInvitationSlide($next, $cursor, $collected);
        } else {
            return $this;
        }
    }
}

<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\SecurityDomain\Infrastructure\Driven;

use Throwable;
use Closure;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlParam;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlTemplate;
use Civi\Lughauth\Shared\Exception\NotFoundException;
use Civi\Lughauth\Shared\Exception\NotUniqueException;
use Civi\Lughauth\Shared\Exception\ConstraintException;
use Civi\Lughauth\Shared\Exception\OptimistLockException;
use Civi\Lughauth\Shared\Exception\NotEmptyChildsException;
use Civi\Lughauth\Features\Access\SecurityDomain\Domain\Gateway\SecurityDomainFilter;
use Civi\Lughauth\Features\Access\SecurityDomain\Domain\Gateway\SecurityDomainCursor;
use Civi\Lughauth\Features\Access\SecurityDomain\Domain\SecurityDomain;
use Civi\Lughauth\Features\Access\SecurityDomain\Domain\SecurityDomainRef;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;

class SecurityDomainPdoConnector
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly SqlTemplate $db,
    ) {
    }
    public function list(?SecurityDomainFilter $filter = null, ?SecurityDomainCursor $sort = null): array
    {
        $this->logDebug("List query for Security domain");
        $span = $this->startSpan("List query for Security domain");
        try {
            $sqlFilter = $this->filter(false, $filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function listForUpdate(?SecurityDomainFilter $filter = null, ?SecurityDomainCursor $sort = null): array
    {
        $this->logDebug("List query for update of Security domain");
        $span = $this->startSpan("List query for update of Security domain");
        try {
            $sqlFilter = $this->filter(true, $filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function query(string $query, ?array $params = []): array
    {
        $this->logDebug("Make query for entities for Security domain");
        $span = $this->startSpan("Make query for entities for Security domain");
        try {
            return $this->db->query($query, $params, fn ($row) => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function rawQuery(string $query, ?array $params = []): array
    {
        $this->logDebug("Make raw query for Security domain");
        $span = $this->startSpan("Make raw query for Security domain");
        try {
            return $this->db->query($query, $params, fn ($row) => $row);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieve(SecurityDomainFilter $filter): ?SecurityDomain
    {
        $this->logDebug("Retrieve query for Security domain");
        $span = $this->startSpan("Retrieve query for Security domain");
        try {
            $sqlFilter = $this->filter(false, $filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn ($row) => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieveForUpdate(SecurityDomainFilter $filter): ?SecurityDomain
    {
        $this->logDebug("Retrieve query for update of Security domain");
        $span = $this->startSpan("Retrieve query for update of Security domain");
        try {
            $sqlFilter = $this->filter(true, $filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn ($row) => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function create(SecurityDomain $entity, ?Closure $verify = null): SecurityDomain
    {
        $this->logDebug("Execute insert sql query for Security domain");
        $span = $this->startSpan("Execute insert sql query for Security domain");
        try {
            try {
                $this->db->execute('INSERT INTO "access_security_domain" ( "uid", "name", "level", "read_all", "write_all", "manage_all", "view_all_attributes", "modify_all_attributes", "enabled", "version") VALUES ( :uid, :name, :level, :readAll, :writeAll, :manageAll, :viewAllAttributes, :modifyAllAttributes, :enabled, :version)', [
                     new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'name', value: $entity->getName(), type: SqlParam::STR),
                     new SqlParam(name: 'level', value: $entity->getLevel(), type: SqlParam::INT),
                     new SqlParam(name: 'readAll', value: $entity->getReadAll(), type: SqlParam::BOOL),
                     new SqlParam(name: 'writeAll', value: $entity->getWriteAll(), type: SqlParam::BOOL),
                     new SqlParam(name: 'manageAll', value: $entity->getManageAll(), type: SqlParam::BOOL),
                     new SqlParam(name: 'viewAllAttributes', value: $entity->getViewAllAttributes(), type: SqlParam::BOOL),
                     new SqlParam(name: 'modifyAllAttributes', value: $entity->getModifyAllAttributes(), type: SqlParam::BOOL),
                     new SqlParam(name: 'enabled', value: $entity->getEnabled(), type: SqlParam::BOOL),
                     new SqlParam(name: 'version', value: 0, type: SqlParam::INT)
                ]);
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($entity, true);
                throw $ex;
            }
            $created = $entity->withVersion(0);
            if ($verify && !$verify($created)) {
                $this->db->execute('DELETE FROM "access_security_domain" where "uid" = :uid', [
                  new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR)
                ]);
                throw new NotFoundException('Insertion of not visible values');
            }
            return $created;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function update(SecurityDomain $update): ?SecurityDomain
    {
        $this->logDebug("Execute update sql query for Security domain");
        $span = $this->startSpan("Execute update sql query for Security domain");
        try {
            try {
                $result = $this->db->execute('UPDATE "access_security_domain" SET "name" = :name , "level" = :level , "read_all" = :readAll , "write_all" = :writeAll , "manage_all" = :manageAll , "view_all_attributes" = :viewAllAttributes , "modify_all_attributes" = :modifyAllAttributes , "enabled" = :enabled , "version" = :version WHERE "uid" = :uid and "version" = :_lock_version', [
                     new SqlParam(name: 'uid', value: $update->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'name', value: $update->getName(), type: SqlParam::STR),
                     new SqlParam(name: 'level', value: $update->getLevel(), type: SqlParam::INT),
                     new SqlParam(name: 'readAll', value: $update->getReadAll(), type: SqlParam::BOOL),
                     new SqlParam(name: 'writeAll', value: $update->getWriteAll(), type: SqlParam::BOOL),
                     new SqlParam(name: 'manageAll', value: $update->getManageAll(), type: SqlParam::BOOL),
                     new SqlParam(name: 'viewAllAttributes', value: $update->getViewAllAttributes(), type: SqlParam::BOOL),
                     new SqlParam(name: 'modifyAllAttributes', value: $update->getModifyAllAttributes(), type: SqlParam::BOOL),
                     new SqlParam(name: 'enabled', value: $update->getEnabled(), type: SqlParam::BOOL),
                     new SqlParam(name: 'version', value: $update->getVersion() + 1, type: SqlParam::INT),
                     new SqlParam(name: '_lock_version', value: $update->getVersion(), type: SqlParam::INT)
                ]);
                if (!$result && $this->db->exists('select "uid" from "access_security_domain" where "uid" = :uid', ['uid' => $update->uid() ])) {
                    throw new OptimistLockException($update->uid(), "version: " . $update->getVersion());
                } elseif (!$result) {
                    throw new NotFoundException($update->uid());
                }
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($update, false);
                throw $ex;
            }
            return $update->withVersion($update->getVersion() + 1);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function delete(SecurityDomainRef $ref): bool
    {
        $this->logDebug("Execute delete sql query for Security domain");
        $span = $this->startSpan("Execute delete sql query for Security domain");
        try {
            try {
                return $this->db->execute('DELETE FROM "access_security_domain" where "uid" = :uid', [new SqlParam(name: 'uid', value: $ref->uid(), type:SqlParam::STR)]);
            } catch (NotEmptyChildsException $ex) {
                throw ConstraintException::ofError('not-empty', ['uid'], [$ref->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function exists(SecurityDomainFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for Security domain");
        $span = $this->startSpan("Execute exists sql query for Security domain");
        try {
            $sqlFilter = $this->filter(false, $filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function existsForUpdate(SecurityDomainFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for update of Security domain");
        $span = $this->startSpan("Execute update sql query for update of Security domain");
        try {
            $sqlFilter = $this->filter(false, $filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function count(?SecurityDomainFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for Security domain");
        $span = $this->startSpan("Execute count sql query for Security domain");
        try {
            $sqlFilter = $this->filter(true, $filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn ($row) => $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function countForUpdate(?SecurityDomainFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for update of Security domain");
        $span = $this->startSpan("Execute count sql query for update of Security domain");
        try {
            $sqlFilter = $this->filter(false, $filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn ($row) => $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function checkDuplicates(SecurityDomain $entity, bool $creation)
    {
        $this->logDebug("Query to check duplicates for Security domain");
        $span = $this->startSpan("Query to check duplicates for Security domain");
        try {
            $values = ['uid' => $entity->uid()];
            if ($creation &&  $this->db->exists('SELECT  "uid" from "access_security_domain" where "uid" = :uid', $values)) {
                throw ConstraintException::ofError('not-unique', array_keys($values), array_values($values));
            }
            $values = ['name' => $entity->getName(), 'uid' => $entity->uid()];
            if ($this->db->exists('SELECT  "name" from "access_security_domain" where "name" = :name and "uid" != :uid', $values)) {
                throw ConstraintException::ofError('not-unique', ['name'], [$entity->getName()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function filter(bool $forUpdate, ?SecurityDomainFilter $filter, ?SecurityDomainCursor $sort, bool $count)
    {
        $this->logDebug("Build query filter of Security domain");
        $span = $this->startSpan("Build query filter of Security domain");
        try {
            $join = '';
            $query = '';
            $params = [];
            $order = '';
            $limit = '';
            if ($filter) {
                $filterUids = $filter->uids();
                if ($filterUids && count($filterUids) > 1) {
                    $query .= ' and "uid" in (:uids)';
                    $params[] = new SqlParam(name:'uids', value: $filterUids, type: SqlParam::STR);
                } elseif ($filterUids) {
                    $query .= ' and "uid" = :uid';
                    $params[] = new SqlParam(name:'uid', value: $filterUids[0], type: SqlParam::STR);
                }
                $filterSearch = $filter->search();
                if ($filterSearch) {
                    $query .= ' and ( "name" like :search)';
                    $params[] = new SqlParam(name:'search', value: '%'. $filterSearch . '%', type: SqlParam::STR);
                }
                $filterName = $filter->name();
                if ($filterName) {
                    $query .= ' and "name" = :name';
                    $params[] = new SqlParam(name: 'name', value: $filterName, type: SqlParam::STR);
                }
                if ($filterEnabled = $filter->enabled()) {
                    $query .= ' and "enabled" = :enabled ';
                    $params[] = new SqlParam(name: 'enabled', value: $filterEnabled, type: SqlParam::BOOL);
                }
                if ($filterName = $filter->name()) {
                    $query .= ' and "name" = :name ';
                    $params[] = new SqlParam(name: 'name', value: $filterName, type: SqlParam::STR);
                }
            }
            if ($sort) {
                $sortLimit = $sort->limit();
                if ($sortLimit) {
                    $limit = ' LIMIT ' . $this->db->escapeValue($sortLimit, SqlParam::INT);
                }
                $sortOrder = $sort->order();
                if ($sortOrder) {
                    foreach ($sortOrder as $ord) {
                        $equals = '';
                        if ($ord === 'nameAsc') {
                            $sortSinceName = $sort->sinceName();
                            if ($sortSinceName) {
                                $query .= " and " . ($equals ? substr($equals, 4) . ' and ' : '') . ' "name" > :sinceName';
                                $equals .= ' and "name" = :sinceName';
                                $params[] = new SqlParam(name: 'sinceName', value: $sortSinceName, type: SqlParam::STR);
                            }
                            $order .= ', "access_security_domain"."name" asc';
                        }
                        if ($ord === 'nameDesc') {
                            $sortSinceName = $sort->sinceName();
                            if ($sortSinceName) {
                                $query .= " and " . ($equals ? substr($equals, 4) . ' and ' : '') . ' "name" < :sinceName';
                                $equals .= ' and "name" = :sinceName';
                                $params[] = new SqlParam(name: 'sinceName', value: $sortSinceName, type: SqlParam::STR);
                            }
                            $order .= ', "access_security_domain"."name" desc';
                        }
                    }
                } else {
                    $sortSinceUid = $sort->sinceUid();
                    if ($sortSinceUid) {
                        $query .= ' and  "uid" < :sinceUid';
                        $params[] = new SqlParam(name: 'sinceUid', value: $sortSinceUid, type: SqlParam::STR);
                    }
                    $order = ', "access_security_domain"."uid" desc';
                }
            }
            return [
              'query' => 'SELECT '.($count ? ' count(*) as count ' : '*').' FROM "access_security_domain"'
                . $join
                . ($query ? ' WHERE ' . substr($query, 4) : '')
                . ($order ? ' ORDER BY ' . substr($order, 2) : '') . $limit . ($forUpdate ? " FOR UPDATE" : ""),
              'params' => $params];
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function mapper($row): SecurityDomain
    {
        $this->logDebug("Mapping from sql to entity for Security domain");
        $span = $this->startSpan("Mapping from sql to enttiy for Security domain");
        try {
            return new SecurityDomain(
                uid: $row['uid'] ?? null,
                name: $row['name'] ?? null,
                level: $row['level'] ?? null,
                readAll: isset($row['read_all']) ? !! $row['read_all'] : null,
                writeAll: isset($row['write_all']) ? !! $row['write_all'] : null,
                manageAll: isset($row['manage_all']) ? !! $row['manage_all'] : null,
                viewAllAttributes: isset($row['view_all_attributes']) ? !! $row['view_all_attributes'] : null,
                modifyAllAttributes: isset($row['modify_all_attributes']) ? !! $row['modify_all_attributes'] : null,
                enabled: isset($row['enabled']) ? !! $row['enabled'] : null,
                version: $row['version'] ?? null,
            );
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

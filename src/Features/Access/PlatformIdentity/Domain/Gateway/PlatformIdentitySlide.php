<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\PlatformIdentity\Domain\Gateway;

use Civi\Lughauth\Features\Access\PlatformIdentity\Domain\PlatformIdentity;

class PlatformIdentitySlide extends \ArrayIterator
{
    private readonly ?PlatformIdentity $last;
    private readonly PlatformIdentityCursor $nextCursor;
    public function __construct(
        private readonly ?\Closure $next,
        private readonly ?PlatformIdentityCursor $cursor,
        private readonly array $values
    ) {
        parent::__construct($values);
        $this->last = $values ? end($values) : null;
        $this->nextCursor = new PlatformIdentityCursor(
            limit: $this->cursor->limit(),
            sinceUid: $this->last ? $this->last->uid() : null,
        );
    }
    public function cursor(): ?PlatformIdentityCursor
    {
        return $this->cursor;
    }
    public function nextCursor(): PlatformIdentityCursor
    {
        return $this->nextCursor;
    }
    public function values(): array
    {
        return $this->values;
    }
    public function current(): PlatformIdentity
    {
        return parent::current();
    }
    public function map(\Closure $fn): PlatformIdentitySlide
    {
        return new PlatformIdentitySlide($this->next, $this->cursor, array_map($fn, $this->values));
    }
    public function slide(\Closure $fn): PlatformIdentitySlide
    {
        $filtered = array_filter($this->values, $fn);
        $collected = $filtered;
        $last = $this->last;
        $next = $this->next;
        $cursor = $this->cursor;
        if (count($collected) == $cursor->limit()) {
            while (
                count($collected) < count($this->values) && $next
            ) {
                $nextSlide = ($this->next)($this, $cursor->next($last, $cursor->limit()));
                if (count($nextSlide)) {
                    $nextFiltered = array_filter($nextSlide->values, $fn);
                    $collected = array_merge($collected, $nextFiltered);
                    $cursor = $nextSlide->cursor;
                    $next = $nextSlide->next;
                    $last = $nextSlide->last;
                } else {
                    break;
                }
            }
            return new PlatformIdentitySlide($next, $cursor, $collected);
        } else {
            return $this;
        }
    }
}

<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\UserMfaRecoveryCode\Infrastructure\Connector\Pdo;

use Throwable;
use Closure;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlParam;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlTemplate;
use Civi\Lughauth\Shared\Exception\NotFoundException;
use Civi\Lughauth\Shared\Exception\NotUniqueException;
use Civi\Lughauth\Shared\Exception\ConstraintException;
use Civi\Lughauth\Shared\Exception\OptimistLockException;
use Civi\Lughauth\Shared\Exception\NotEmptyChildsException;
use Civi\Lughauth\Features\Access\UserMfaRecoveryCode\Domain\Gateway\UserMfaRecoveryCodeFilter;
use Civi\Lughauth\Features\Access\UserMfaRecoveryCode\Domain\Gateway\UserMfaRecoveryCodeCursor;
use Civi\Lughauth\Features\Access\UserMfaRecoveryCode\Domain\UserMfaRecoveryCode;
use Civi\Lughauth\Features\Access\UserMfaRecoveryCode\Domain\UserMfaRecoveryCodeRef;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;
use Civi\Lughauth\Features\Access\User\Domain\UserRef;

class UserMfaRecoveryCodePdoConnector
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly SqlTemplate $db,
    ) {
    }
    public function list(?UserMfaRecoveryCodeFilter $filter = null, ?UserMfaRecoveryCodeCursor $sort = null): array
    {
        $this->logDebug("List query for User mfa recovery code");
        $span = $this->startSpan("List query for User mfa recovery code");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function listForUpdate(?UserMfaRecoveryCodeFilter $filter = null, ?UserMfaRecoveryCodeCursor $sort = null): array
    {
        $this->logDebug("List query for update of User mfa recovery code");
        $span = $this->startSpan("List query for update of User mfa recovery code");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->queryForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function query(string $query, ?array $params = []): array
    {
        return $this->theQuery(false, $query, $params);
    }
    private function rawQuery(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(false, $query, $params);
    }
    private function queryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theQuery(true, $query, $params);
    }
    private function rawQueryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(true, $query, $params);
    }
    private function theQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make query for entities for User mfa recovery code");
        $span = $this->startSpan("Make query for entities for User mfa recovery code");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): UserMfaRecoveryCode => $this->mapper($row)) : $this->db->query($query, $params, fn (array $row): UserMfaRecoveryCode => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function theRawQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make raw query for User mfa recovery code");
        $span = $this->startSpan("Make raw query for User mfa recovery code");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): array => $row) : $this->db->query($query, $params, fn (array $row): array => $row);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieve(UserMfaRecoveryCodeFilter $filter): ?UserMfaRecoveryCode
    {
        $this->logDebug("Retrieve query for User mfa recovery code");
        $span = $this->startSpan("Retrieve query for User mfa recovery code");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): UserMfaRecoveryCode => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieveForUpdate(UserMfaRecoveryCodeFilter $filter): ?UserMfaRecoveryCode
    {
        $this->logDebug("Retrieve query for update of User mfa recovery code");
        $span = $this->startSpan("Retrieve query for update of User mfa recovery code");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row): UserMfaRecoveryCode => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function create(UserMfaRecoveryCode $entity, ?Closure $verify = null): UserMfaRecoveryCode
    {
        $this->logDebug("Execute insert sql query for User mfa recovery code");
        $span = $this->startSpan("Execute insert sql query for User mfa recovery code");
        try {
            try {
                $this->db->execute('INSERT INTO "access_user_mfa_recovery_code" ( "uid", "user", "code_hash", "used_at", "created_at", "version") VALUES ( :uid, :user, :codeHash, :usedAt, :createdAt, :version)', [
                     new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'user', value: $entity->getUser()->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'codeHash', value: $entity->getCodeHash(), type: SqlParam::STR),
                     new SqlParam(name: 'usedAt', value: $entity->getUsedAt(), type: SqlParam::STR),
                     new SqlParam(name: 'createdAt', value: $entity->getCreatedAt(), type: SqlParam::STR),
                     new SqlParam(name: 'version', value: 0, type: SqlParam::INT)
                ]);
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($entity, true);
                throw $ex;
            }
            $created = $entity->withVersion(0);
            if ($verify && !$verify($created)) {
                $this->db->execute('DELETE FROM "access_user_mfa_recovery_code" where "uid" = :uid', [
                  new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR)
                ]);
                throw new NotFoundException('Insertion of not visible values');
            }
            return $created;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function update(UserMfaRecoveryCode $update): UserMfaRecoveryCode
    {
        $this->logDebug("Execute update sql query for User mfa recovery code");
        $span = $this->startSpan("Execute update sql query for User mfa recovery code");
        try {
            try {
                $result = $this->db->execute('UPDATE "access_user_mfa_recovery_code" SET "user" = :user , "code_hash" = :codeHash , "used_at" = :usedAt , "created_at" = :createdAt , "version" = :version WHERE "uid" = :uid and "version" = :_lock_version', [
                     new SqlParam(name: 'uid', value: $update->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'user', value: $update->getUser()->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'codeHash', value: $update->getCodeHash(), type: SqlParam::STR),
                     new SqlParam(name: 'usedAt', value: $update->getUsedAt(), type: SqlParam::STR),
                     new SqlParam(name: 'createdAt', value: $update->getCreatedAt(), type: SqlParam::STR),
                     new SqlParam(name: 'version', value: ($update->getVersion() ?? 0) + 1, type: SqlParam::INT),
                     new SqlParam(name: '_lock_version', value: $update->getVersion(), type: SqlParam::INT)
                ]);
                if (!$result && $this->db->exists('select "uid" from "access_user_mfa_recovery_code" where "uid" = :uid', ['uid' => $update->uid() ])) {
                    throw new OptimistLockException($update->uid() ?? 'no-id', "version: " . ($update->getVersion() ?? 0));
                } elseif (!$result) {
                    throw new NotFoundException($update->uid() ?? 'no-id');
                }
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($update, false);
                throw $ex;
            }
            return $update->withVersion(($update->getVersion() ?? 0) + 1);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function delete(UserMfaRecoveryCodeRef $ref): bool
    {
        $this->logDebug("Execute delete sql query for User mfa recovery code");
        $span = $this->startSpan("Execute delete sql query for User mfa recovery code");
        try {
            try {
                return $this->db->execute('DELETE FROM "access_user_mfa_recovery_code" where "uid" = :uid', [new SqlParam(name: 'uid', value: $ref->uid(), type:SqlParam::STR)]);
            } catch (NotEmptyChildsException $ex) {
                throw ConstraintException::ofError('not-empty', ['uid'], [$ref->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function exists(UserMfaRecoveryCodeFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for User mfa recovery code");
        $span = $this->startSpan("Execute exists sql query for User mfa recovery code");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function existsForUpdate(UserMfaRecoveryCodeFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for update of User mfa recovery code");
        $span = $this->startSpan("Execute update sql query for update of User mfa recovery code");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->existsForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function count(?UserMfaRecoveryCodeFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for User mfa recovery code");
        $span = $this->startSpan("Execute count sql query for User mfa recovery code");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): int => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function countForUpdate(?UserMfaRecoveryCodeFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for update of User mfa recovery code");
        $span = $this->startSpan("Execute count sql query for update of User mfa recovery code");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row) => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function checkDuplicates(UserMfaRecoveryCode $entity, bool $creation): void
    {
        $this->logDebug("Query to check duplicates for User mfa recovery code");
        $span = $this->startSpan("Query to check duplicates for User mfa recovery code");
        try {
            $values = ['uid' => $entity->uid()];
            if ($creation &&  $this->db->exists('SELECT  "uid" from "access_user_mfa_recovery_code" where "uid" = :uid', $values)) {
                throw ConstraintException::ofError('not-unique', array_keys($values), array_values($values));
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function filter(?UserMfaRecoveryCodeFilter $filter, ?UserMfaRecoveryCodeCursor $sort, bool $count): array
    {
        $this->logDebug("Build query filter of User mfa recovery code");
        $span = $this->startSpan("Build query filter of User mfa recovery code");
        try {
            $join = '';
            $query = '';
            $params = [];
            $order = '';
            $limit = '';
            if ($filter) {
                $filterUids = $filter->uids();
                if (null !== $filterUids && count($filterUids) > 1) {
                    $query .= ' and "access_user_mfa_recovery_code"."uid" in (:uids)';
                    $params[] = new SqlParam(name:'uids', value: $filterUids, type: SqlParam::STR);
                } elseif (null !== $filterUids) {
                    $query .= ' and "access_user_mfa_recovery_code"."uid" = :uid';
                    $params[] = new SqlParam(name:'uid', value: $filterUids[0], type: SqlParam::STR);
                }
                $filterSearch = $filter->search();
                if (null !== $filterSearch) {
                    $query .= ' and ( "access_user_mfa_recovery_code"."uid" like :search)';
                    $params[] = new SqlParam(name:'search', value: '%'. $filterSearch . '%', type: SqlParam::STR);
                }
                $filterUserUnused = $filter->userUnused();
                if (null !== $filterUserUnused) {
                    $query .= ' and ("access_user_mfa_recovery_code"."user" = :userUserUnused and "access_user_mfa_recovery_code"."used_at" is null)';
                    $params[] = new SqlParam(name: 'userUserUnused', value: $filterUserUnused, type: SqlParam::STR);
                }
                $filterUser = $filter->user();
                if (null !== $filterUser) {
                    $query .= ' and "access_user_mfa_recovery_code"."user" = :user ';
                    $params[] = new SqlParam(name: 'user', value: $filterUser->uid(), type: SqlParam::STR);
                }
                $filterUserTenantAccesible = $filter->userTenantAccesible();
                if (null !== $filterUserTenantAccesible) {
                    $join .= ' LEFT JOIN "access_user" as "userTenantAccesibleUser" ON "userTenantAccesibleUser"."uid" = "access_user_mfa_recovery_code"."user" LEFT JOIN "access_tenant" as "userTenantAccesibleTenant" ON "userTenantAccesibleTenant"."uid" = "userTenantAccesibleUser"."tenant"';
                    $query .= ' and "userTenantAccesibleTenant"."uid" = :userTenantAccesible';
                    $params[] = new SqlParam(name: 'userTenantAccesible', value: $filterUserTenantAccesible, type: SqlParam::STR);
                }
            }
            if ($sort) {
                $sortLimit = $sort->limit();
                if ($sortLimit) {
                    $limit = ' LIMIT ' . $this->db->escapeValue($sortLimit, SqlParam::INT);
                }
                $sortSinceUid = $sort->sinceUid();
                if (null !== $sortSinceUid) {
                    $query .= ' and  "uid" < :sinceUid';
                    $params[] = new SqlParam(name: 'sinceUid', value: $sortSinceUid, type: SqlParam::STR);
                }
                $order = ', "access_user_mfa_recovery_code"."uid" desc';
            }
            return [
              'query' => 'SELECT '.($count ? ' count("access_user_mfa_recovery_code".*) as count ' : '"access_user_mfa_recovery_code".*').' FROM "access_user_mfa_recovery_code"'
                . $join
                . ($query ? ' WHERE ' . substr($query, 4) : '')
                . ($order ? ' ORDER BY ' . substr($order, 2) : '') . $limit,
              'params' => $params];
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function mapper(array $row): UserMfaRecoveryCode
    {
        $this->logDebug("Mapping from sql to entity for User mfa recovery code");
        $span = $this->startSpan("Mapping from sql to entity for User mfa recovery code");
        try {
            $uid = $row['uid'] ?? null;
            if (null === $uid) {
                throw ConstraintException::ofError('not-null', ['uid'], [null]);
            }
            $rawUser = $row['user'] ?? null;
            if (null === $rawUser) {
                throw ConstraintException::ofError('not-null', ['user'], [null]);
            }
            $user = new UserRef(uid: $rawUser);
            $codeHash = $row['code_hash'] ?? null;
            if (null === $codeHash) {
                throw ConstraintException::ofError('not-null', ['codeHash'], [null]);
            }
            $usedAt = $row['used_at'] ? new \DateTimeImmutable($row['used_at']) : null;
            $createdAt = $row['created_at'] ? new \DateTimeImmutable($row['created_at']) : null;
            if (null === $createdAt) {
                throw ConstraintException::ofError('not-null', ['createdAt'], [null]);
            }
            $version = $row['version'] ?? null;
            return new UserMfaRecoveryCode(
                uid: $uid,
                user: $user,
                codeHash: $codeHash,
                usedAt: $usedAt,
                createdAt: $createdAt,
                version: $version,
            );
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

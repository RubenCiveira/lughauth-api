<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\UserAccessTemporalCode\Infrastructure\Driven;

use Throwable;
use Closure;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlParam;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlTemplate;
use Civi\Lughauth\Shared\Exception\NotFoundException;
use Civi\Lughauth\Shared\Exception\NotUniqueException;
use Civi\Lughauth\Shared\Exception\ConstraintException;
use Civi\Lughauth\Shared\Exception\OptimistLockException;
use Civi\Lughauth\Shared\Exception\NotEmptyChildsException;
use Civi\Lughauth\Features\Access\UserAccessTemporalCode\Domain\Gateway\UserAccessTemporalCodeFilter;
use Civi\Lughauth\Features\Access\UserAccessTemporalCode\Domain\Gateway\UserAccessTemporalCodeCursor;
use Civi\Lughauth\Features\Access\UserAccessTemporalCode\Domain\UserAccessTemporalCode;
use Civi\Lughauth\Features\Access\UserAccessTemporalCode\Domain\UserAccessTemporalCodeRef;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;
use Civi\Lughauth\Features\Access\User\Domain\UserRef;
use Civi\Lughauth\Features\Access\UserAccessTemporalCode\Domain\ValueObject\UserAccessTemporalCodeTempSecondFactorSeedVO;
use Civi\Lughauth\Shared\Security\AesCypherService;

class UserAccessTemporalCodePdoConnector
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly AesCypherService $cypher,
        private readonly SqlTemplate $db,
    ) {
    }
    public function list(?UserAccessTemporalCodeFilter $filter = null, ?UserAccessTemporalCodeCursor $sort = null): array
    {
        $this->logDebug("List query for User access temporal code");
        $span = $this->startSpan("List query for User access temporal code");
        try {
            $sqlFilter = $this->filter(false, $filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function listForUpdate(?UserAccessTemporalCodeFilter $filter = null, ?UserAccessTemporalCodeCursor $sort = null): array
    {
        $this->logDebug("List query for update of User access temporal code");
        $span = $this->startSpan("List query for update of User access temporal code");
        try {
            $sqlFilter = $this->filter(true, $filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function query(string $query, ?array $params = []): array
    {
        $this->logDebug("Make query for entities for User access temporal code");
        $span = $this->startSpan("Make query for entities for User access temporal code");
        try {
            return $this->db->query($query, $params, fn ($row) => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function rawQuery(string $query, ?array $params = []): array
    {
        $this->logDebug("Make raw query for User access temporal code");
        $span = $this->startSpan("Make raw query for User access temporal code");
        try {
            return $this->db->query($query, $params, fn ($row) => $row);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieve(UserAccessTemporalCodeFilter $filter): ?UserAccessTemporalCode
    {
        $this->logDebug("Retrieve query for User access temporal code");
        $span = $this->startSpan("Retrieve query for User access temporal code");
        try {
            $sqlFilter = $this->filter(false, $filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn ($row) => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieveForUpdate(UserAccessTemporalCodeFilter $filter): ?UserAccessTemporalCode
    {
        $this->logDebug("Retrieve query for update of User access temporal code");
        $span = $this->startSpan("Retrieve query for update of User access temporal code");
        try {
            $sqlFilter = $this->filter(true, $filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn ($row) => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function create(UserAccessTemporalCode $entity, ?Closure $verify = null): UserAccessTemporalCode
    {
        $this->logDebug("Execute insert sql query for User access temporal code");
        $span = $this->startSpan("Execute insert sql query for User access temporal code");
        try {
            try {
                $this->db->execute('INSERT INTO "access_user_access_temporal_code" ( "uid", "user", "temp_second_factor_seed", "temp_second_factor_seed_expiration", "failed_login_attempts", "recovery_code", "recovery_code_expiration", "version") VALUES ( :uid, :user, :tempSecondFactorSeed, :tempSecondFactorSeedExpiration, :failedLoginAttempts, :recoveryCode, :recoveryCodeExpiration, :version)', [
                     new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'user', value: $entity->getUser()?->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'tempSecondFactorSeed', value: $entity->getCypheredTempSecondFactorSeed($this->cypher), type: SqlParam::STR),
                     new SqlParam(name: 'tempSecondFactorSeedExpiration', value: $entity->getTempSecondFactorSeedExpiration(), type: SqlParam::STR),
                     new SqlParam(name: 'failedLoginAttempts', value: $entity->getFailedLoginAttempts(), type: SqlParam::INT),
                     new SqlParam(name: 'recoveryCode', value: $entity->getRecoveryCode(), type: SqlParam::STR),
                     new SqlParam(name: 'recoveryCodeExpiration', value: $entity->getRecoveryCodeExpiration(), type: SqlParam::STR),
                     new SqlParam(name: 'version', value: 0, type: SqlParam::INT)
                ]);
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($entity, true);
                throw $ex;
            }
            $created = $entity->withVersion(0);
            if ($verify && !$verify($created)) {
                $this->db->execute('DELETE FROM "access_user_access_temporal_code" where "uid" = :uid', [
                  new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR)
                ]);
                throw new NotFoundException('Insertion of not visible values');
            }
            return $created;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function update(UserAccessTemporalCode $update): ?UserAccessTemporalCode
    {
        $this->logDebug("Execute update sql query for User access temporal code");
        $span = $this->startSpan("Execute update sql query for User access temporal code");
        try {
            try {
                $result = $this->db->execute('UPDATE "access_user_access_temporal_code" SET "user" = :user , "temp_second_factor_seed" = :tempSecondFactorSeed , "temp_second_factor_seed_expiration" = :tempSecondFactorSeedExpiration , "failed_login_attempts" = :failedLoginAttempts , "recovery_code" = :recoveryCode , "recovery_code_expiration" = :recoveryCodeExpiration , "version" = :version WHERE "uid" = :uid and "version" = :_lock_version', [
                     new SqlParam(name: 'uid', value: $update->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'user', value: $update->getUser()?->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'tempSecondFactorSeed', value: $update->getCypheredTempSecondFactorSeed($this->cypher), type: SqlParam::STR),
                     new SqlParam(name: 'tempSecondFactorSeedExpiration', value: $update->getTempSecondFactorSeedExpiration(), type: SqlParam::STR),
                     new SqlParam(name: 'failedLoginAttempts', value: $update->getFailedLoginAttempts(), type: SqlParam::INT),
                     new SqlParam(name: 'recoveryCode', value: $update->getRecoveryCode(), type: SqlParam::STR),
                     new SqlParam(name: 'recoveryCodeExpiration', value: $update->getRecoveryCodeExpiration(), type: SqlParam::STR),
                     new SqlParam(name: 'version', value: $update->getVersion() + 1, type: SqlParam::INT),
                     new SqlParam(name: '_lock_version', value: $update->getVersion(), type: SqlParam::INT)
                ]);
                if (!$result && $this->db->exists('select "uid" from "access_user_access_temporal_code" where "uid" = :uid', ['uid' => $update->uid() ])) {
                    throw new OptimistLockException($update->uid(), "version: " . $update->getVersion());
                } elseif (!$result) {
                    throw new NotFoundException($update->uid());
                }
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($update, false);
                throw $ex;
            }
            return $update->withVersion($update->getVersion() + 1);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function delete(UserAccessTemporalCodeRef $ref): bool
    {
        $this->logDebug("Execute delete sql query for User access temporal code");
        $span = $this->startSpan("Execute delete sql query for User access temporal code");
        try {
            try {
                return $this->db->execute('DELETE FROM "access_user_access_temporal_code" where "uid" = :uid', [new SqlParam(name: 'uid', value: $ref->uid(), type:SqlParam::STR)]);
            } catch (NotEmptyChildsException $ex) {
                throw ConstraintException::ofError('not-empty', ['uid'], [$ref->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function exists(UserAccessTemporalCodeFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for User access temporal code");
        $span = $this->startSpan("Execute exists sql query for User access temporal code");
        try {
            $sqlFilter = $this->filter(false, $filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function existsForUpdate(UserAccessTemporalCodeFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for update of User access temporal code");
        $span = $this->startSpan("Execute update sql query for update of User access temporal code");
        try {
            $sqlFilter = $this->filter(false, $filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function count(?UserAccessTemporalCodeFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for User access temporal code");
        $span = $this->startSpan("Execute count sql query for User access temporal code");
        try {
            $sqlFilter = $this->filter(true, $filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn ($row) => $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function countForUpdate(?UserAccessTemporalCodeFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for update of User access temporal code");
        $span = $this->startSpan("Execute count sql query for update of User access temporal code");
        try {
            $sqlFilter = $this->filter(false, $filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn ($row) => $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function checkDuplicates(UserAccessTemporalCode $entity, bool $creation)
    {
        $this->logDebug("Query to check duplicates for User access temporal code");
        $span = $this->startSpan("Query to check duplicates for User access temporal code");
        try {
            $values = ['uid' => $entity->uid()];
            if ($creation &&  $this->db->exists('SELECT  "uid" from "access_user_access_temporal_code" where "uid" = :uid', $values)) {
                throw ConstraintException::ofError('not-unique', array_keys($values), array_values($values));
            }
            $values = ['user' => $entity->getUser()?->uid(), 'uid' => $entity->uid()];
            if ($this->db->exists('SELECT  "user" from "access_user_access_temporal_code" where "user" = :user and "uid" != :uid', $values)) {
                throw ConstraintException::ofError('not-unique', ['user'], [$entity->getUser()?->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function filter(bool $forUpdate, ?UserAccessTemporalCodeFilter $filter, ?UserAccessTemporalCodeCursor $sort, bool $count)
    {
        $this->logDebug("Build query filter of User access temporal code");
        $span = $this->startSpan("Build query filter of User access temporal code");
        try {
            $join = '';
            $query = '';
            $params = [];
            $order = '';
            $limit = '';
            if ($filter) {
                $filterUids = $filter->uids();
                if ($filterUids && count($filterUids) > 1) {
                    $query .= ' and "uid" in (:uids)';
                    $params[] = new SqlParam(name:'uids', value: $filterUids, type: SqlParam::STR);
                } elseif ($filterUids) {
                    $query .= ' and "uid" = :uid';
                    $params[] = new SqlParam(name:'uid', value: $filterUids[0], type: SqlParam::STR);
                }
                $filterSearch = $filter->search();
                if ($filterSearch) {
                    $query .= ' and ( "uid" like :search)';
                    $params[] = new SqlParam(name:'search', value: '%'. $filterSearch . '%', type: SqlParam::STR);
                }
                $filterUser = $filter->user();
                if ($filterUser) {
                    $query .= ' and "user" = :user';
                    $params[] = new SqlParam(name: 'user', value: $filterUser->uid(), type: SqlParam::STR);
                }
                if ($filterUser = $filter->user()) {
                    $query .= ' and "user" = :user ';
                    $params[] = new SqlParam(name: 'user', value: $filterUser->uid(), type: SqlParam::STR);
                }
                if ($filterUsers = $filter->users()) {
                    $query .= ' and "user" in (:users)  ';
                    $params[] = new SqlParam(name: 'users', value: $filterUsers, type: SqlParam::STR);
                }
                if ($filterUserTenantTenantAccesible = $filter->userTenantTenantAccesible()) {
                    $join .= ' LEFT JOIN "access_user" as "userTenantTenantAccesibleUser" ON "userTenantTenantAccesibleUser"."uid" = "access_user_access_temporal_code"."user" LEFT JOIN "access_tenant" as "userTenantTenantAccesibleTenant" ON "userTenantTenantAccesibleTenant"."uid" = "userTenantTenantAccesibleUser"."tenant"';
                    $query .= ' and "userTenantTenantAccesibleUser"."userTenantTenantAccesibleTenant"."name" = :userTenantTenantAccesible';
                    $params[] = new SqlParam(name: 'userTenantTenantAccesible', value: $filterUserTenantTenantAccesible, type: SqlParam::STR);
                }
            }
            if ($sort) {
                $sortLimit = $sort->limit();
                if ($sortLimit) {
                    $limit = ' LIMIT ' . $this->db->escapeValue($sortLimit, SqlParam::INT);
                }
                $sortSinceUid = $sort->sinceUid();
                if ($sortSinceUid) {
                    $query .= ' and  "uid" < :sinceUid';
                    $params[] = new SqlParam(name: 'sinceUid', value: $sortSinceUid, type: SqlParam::STR);
                }
                $order = ', "access_user_access_temporal_code"."uid" desc';
            }
            return [
              'query' => 'SELECT '.($count ? ' count(*) as count ' : '*').' FROM "access_user_access_temporal_code"'
                . $join
                . ($query ? ' WHERE ' . substr($query, 4) : '')
                . ($order ? ' ORDER BY ' . substr($order, 2) : '') . $limit . ($forUpdate ? " FOR UPDATE" : ""),
              'params' => $params];
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function mapper($row): UserAccessTemporalCode
    {
        $this->logDebug("Mapping from sql to entity for User access temporal code");
        $span = $this->startSpan("Mapping from sql to enttiy for User access temporal code");
        try {
            return new UserAccessTemporalCode(
                uid: $row['uid'] ?? null,
                user: isset($row['user']) ? new UserRef(uid: $row['user']) : null,
                tempSecondFactorSeed: isset($row['temp_second_factor_seed']) && $row['temp_second_factor_seed'] ? UserAccessTemporalCodeTempSecondFactorSeedVO::fromCypheredText($this->cypher, $row['temp_second_factor_seed']) : UserAccessTemporalCodeTempSecondFactorSeedVO::empty(),
                tempSecondFactorSeedExpiration: $row['temp_second_factor_seed_expiration'] ? new \DateTimeImmutable($row['temp_second_factor_seed_expiration']) : null,
                failedLoginAttempts: $row['failed_login_attempts'] ?? null,
                recoveryCode: $row['recovery_code'] ?? null,
                recoveryCodeExpiration: $row['recovery_code_expiration'] ? new \DateTimeImmutable($row['recovery_code_expiration']) : null,
                version: $row['version'] ?? null,
            );
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\Master\TenantLoginProvider\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;

class TenantLoginProviderDisabledVO
{
    public static function empty(): TenantLoginProviderDisabledVO
    {
        return new TenantLoginProviderDisabledVO(null);
    }
    public static function from(TenantLoginProviderDisabledVO|bool|null $value): TenantLoginProviderDisabledVO
    {
        if (is_a($value, TenantLoginProviderDisabledVO::class)) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            return $candidate;
        }
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?TenantLoginProviderDisabledVO
    {
        if (is_a($value, TenantLoginProviderDisabledVO::class)) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new TenantLoginProviderDisabledVO($value);
        } elseif (is_bool($value)) {
            return new TenantLoginProviderDisabledVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['disabled'], [$value], ['bool']));
            return null;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?bool $disabled
    ) {
    }
    public function value(): ?bool
    {
        return $this->disabled;
    }
}

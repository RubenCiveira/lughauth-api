<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\Master\ApiKeyClient\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;
use Civi\Lughauth\Shared\Value\Validation\Rule\Length;

class ApiKeyClientScopesVO
{
    public static function empty(): ApiKeyClientScopesVO
    {
        return new ApiKeyClientScopesVO(null);
    }
    public static function from(ApiKeyClientScopesVO|string|null $value): ApiKeyClientScopesVO
    {
        if (is_a($value, ApiKeyClientScopesVO::class)) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            return $candidate;
        }
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?ApiKeyClientScopesVO
    {
        if (is_a($value, ApiKeyClientScopesVO::class)) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new ApiKeyClientScopesVO($value);
        } elseif (is_string($value)) {
            // If is a ValueObject, we need to append all the errors for the context
            $valid = true;
            foreach (self::rules() as $rule) {
                if ($fail = $rule->check($value)) {
                    $list->add(ConstraintFail::fromRuleFail('scopes', $fail));
                    $valid = false;
                }
            }
            return $valid ? new ApiKeyClientScopesVO($value) : null;
        } else {
            $list->add(new ConstraintFail('wrong_type', ['scopes'], [$value], ['string']));
            return null;
        }
    }
    public static function rules(): array
    {
        return [
          new Length(min: null, max: 250),
        ];
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?string $scopes
    ) {
    }
    public function value(): ?string
    {
        return $this->scopes;
    }
}

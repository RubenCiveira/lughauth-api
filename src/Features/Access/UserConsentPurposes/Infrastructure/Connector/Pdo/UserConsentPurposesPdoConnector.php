<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\UserConsentPurposes\Infrastructure\Connector\Pdo;

use Throwable;
use Closure;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlParam;
use Civi\Lughauth\Shared\Infrastructure\Sql\SqlTemplate;
use Civi\Lughauth\Shared\Exception\NotFoundException;
use Civi\Lughauth\Shared\Exception\NotUniqueException;
use Civi\Lughauth\Shared\Exception\ConstraintException;
use Civi\Lughauth\Shared\Exception\OptimistLockException;
use Civi\Lughauth\Shared\Exception\NotEmptyChildsException;
use Civi\Lughauth\Features\Access\UserConsentPurposes\Domain\Gateway\UserConsentPurposesFilter;
use Civi\Lughauth\Features\Access\UserConsentPurposes\Domain\Gateway\UserConsentPurposesCursor;
use Civi\Lughauth\Features\Access\UserConsentPurposes\Domain\UserConsentPurposes;
use Civi\Lughauth\Features\Access\UserConsentPurposes\Domain\UserConsentPurposesRef;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;
use Civi\Lughauth\Features\Access\User\Domain\UserRef;
use Civi\Lughauth\Features\Access\ConsentPurpose\Domain\ConsentPurposeRef;

class UserConsentPurposesPdoConnector
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly SqlTemplate $db,
    ) {
    }
    public function list(?UserConsentPurposesFilter $filter = null, ?UserConsentPurposesCursor $sort = null): array
    {
        $this->logDebug("List query for User consent purposes");
        $span = $this->startSpan("List query for User consent purposes");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->query($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function listForUpdate(?UserConsentPurposesFilter $filter = null, ?UserConsentPurposesCursor $sort = null): array
    {
        $this->logDebug("List query for update of User consent purposes");
        $span = $this->startSpan("List query for update of User consent purposes");
        try {
            $sqlFilter = $this->filter($filter, $sort, false);
            return $this->queryForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function query(string $query, ?array $params = []): array
    {
        return $this->theQuery(false, $query, $params);
    }
    private function rawQuery(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(false, $query, $params);
    }
    private function queryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theQuery(true, $query, $params);
    }
    private function rawQueryForUpdate(string $query, ?array $params = []): array
    {
        return $this->theRawQuery(true, $query, $params);
    }
    private function theQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make query for entities for User consent purposes");
        $span = $this->startSpan("Make query for entities for User consent purposes");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): UserConsentPurposes => $this->mapper($row)) : $this->db->query($query, $params, fn (array $row): UserConsentPurposes => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function theRawQuery(bool $forUpdate, string $query, ?array $params = []): array
    {
        $this->logDebug("Make raw query for User consent purposes");
        $span = $this->startSpan("Make raw query for User consent purposes");
        try {
            return $forUpdate ? $this->db->queryForUpdate($query, $params, fn (array $row): array => $row) : $this->db->query($query, $params, fn (array $row): array => $row);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieve(UserConsentPurposesFilter $filter): ?UserConsentPurposes
    {
        $this->logDebug("Retrieve query for User consent purposes");
        $span = $this->startSpan("Retrieve query for User consent purposes");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): UserConsentPurposes => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function retrieveForUpdate(UserConsentPurposesFilter $filter): ?UserConsentPurposes
    {
        $this->logDebug("Retrieve query for update of User consent purposes");
        $span = $this->startSpan("Retrieve query for update of User consent purposes");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row): UserConsentPurposes => $this->mapper($row));
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function create(UserConsentPurposes $entity, ?Closure $verify = null): UserConsentPurposes
    {
        $this->logDebug("Execute insert sql query for User consent purposes");
        $span = $this->startSpan("Execute insert sql query for User consent purposes");
        try {
            try {
                $this->db->execute('INSERT INTO "access_user_consent_purposes" ( "uid", "user", "consent_purpose", "granted", "decision_at", "ip_address", "user_agent", "version") VALUES ( :uid, :user, :consentPurpose, :granted, :decisionAt, :ipAddress, :userAgent, :version)', [
                     new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'user', value: $entity->getUser()->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'consentPurpose', value: $entity->getConsentPurpose()->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'granted', value: $entity->isGranted(), type: SqlParam::BOOL),
                     new SqlParam(name: 'decisionAt', value: $entity->getDecisionAt(), type: SqlParam::STR),
                     new SqlParam(name: 'ipAddress', value: $entity->getIpAddress(), type: SqlParam::STR),
                     new SqlParam(name: 'userAgent', value: $entity->getUserAgent(), type: SqlParam::STR),
                     new SqlParam(name: 'version', value: 0, type: SqlParam::INT)
                ]);
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($entity, true);
                throw $ex;
            }
            $created = $entity->withVersion(0);
            if ($verify && !$verify($created)) {
                $this->db->execute('DELETE FROM "access_user_consent_purposes" where "uid" = :uid', [
                  new SqlParam(name: 'uid', value: $entity->uid(), type: SqlParam::STR)
                ]);
                throw new NotFoundException('Insertion of not visible values');
            }
            return $created;
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function update(UserConsentPurposes $update): UserConsentPurposes
    {
        $this->logDebug("Execute update sql query for User consent purposes");
        $span = $this->startSpan("Execute update sql query for User consent purposes");
        try {
            try {
                $result = $this->db->execute('UPDATE "access_user_consent_purposes" SET "user" = :user , "consent_purpose" = :consentPurpose , "granted" = :granted , "decision_at" = :decisionAt , "ip_address" = :ipAddress , "user_agent" = :userAgent , "version" = :version WHERE "uid" = :uid and "version" = :_lock_version', [
                     new SqlParam(name: 'uid', value: $update->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'user', value: $update->getUser()->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'consentPurpose', value: $update->getConsentPurpose()->uid(), type: SqlParam::STR),
                     new SqlParam(name: 'granted', value: $update->isGranted(), type: SqlParam::BOOL),
                     new SqlParam(name: 'decisionAt', value: $update->getDecisionAt(), type: SqlParam::STR),
                     new SqlParam(name: 'ipAddress', value: $update->getIpAddress(), type: SqlParam::STR),
                     new SqlParam(name: 'userAgent', value: $update->getUserAgent(), type: SqlParam::STR),
                     new SqlParam(name: 'version', value: ($update->getVersion() ?? 0) + 1, type: SqlParam::INT),
                     new SqlParam(name: '_lock_version', value: $update->getVersion(), type: SqlParam::INT)
                ]);
                if (!$result && $this->db->exists('select "uid" from "access_user_consent_purposes" where "uid" = :uid', ['uid' => $update->uid() ])) {
                    throw new OptimistLockException($update->uid() ?? 'no-id', "version: " . ($update->getVersion() ?? 0));
                } elseif (!$result) {
                    throw new NotFoundException($update->uid() ?? 'no-id');
                }
            } catch (NotUniqueException $ex) {
                $this->checkDuplicates($update, false);
                throw $ex;
            }
            return $update->withVersion(($update->getVersion() ?? 0) + 1);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function delete(UserConsentPurposesRef $ref): bool
    {
        $this->logDebug("Execute delete sql query for User consent purposes");
        $span = $this->startSpan("Execute delete sql query for User consent purposes");
        try {
            try {
                return $this->db->execute('DELETE FROM "access_user_consent_purposes" where "uid" = :uid', [new SqlParam(name: 'uid', value: $ref->uid(), type:SqlParam::STR)]);
            } catch (NotEmptyChildsException $ex) {
                throw ConstraintException::ofError('not-empty', ['uid'], [$ref->uid()]);
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function exists(UserConsentPurposesFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for User consent purposes");
        $span = $this->startSpan("Execute exists sql query for User consent purposes");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->exists($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function existsForUpdate(UserConsentPurposesFilter $filter): bool
    {
        $this->logDebug("Execute exists sql query for update of User consent purposes");
        $span = $this->startSpan("Execute update sql query for update of User consent purposes");
        try {
            $sqlFilter = $this->filter($filter, null, false);
            return $this->db->existsForUpdate($sqlFilter['query'], $sqlFilter['params']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function count(?UserConsentPurposesFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for User consent purposes");
        $span = $this->startSpan("Execute count sql query for User consent purposes");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOne($sqlFilter['query'], $sqlFilter['params'], fn (array $row): int => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function countForUpdate(?UserConsentPurposesFilter $filter = null): int
    {
        $this->logDebug("Execute count sql query for update of User consent purposes");
        $span = $this->startSpan("Execute count sql query for update of User consent purposes");
        try {
            $sqlFilter = $this->filter($filter, null, true);
            return $this->db->findOneForUpdate($sqlFilter['query'], $sqlFilter['params'], fn (array $row) => (int) $row['count']);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function checkDuplicates(UserConsentPurposes $entity, bool $creation): void
    {
        $this->logDebug("Query to check duplicates for User consent purposes");
        $span = $this->startSpan("Query to check duplicates for User consent purposes");
        try {
            $values = ['uid' => $entity->uid()];
            if ($creation &&  $this->db->exists('SELECT  "uid" from "access_user_consent_purposes" where "uid" = :uid', $values)) {
                throw ConstraintException::ofError('not-unique', array_keys($values), array_values($values));
            }
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function filter(?UserConsentPurposesFilter $filter, ?UserConsentPurposesCursor $sort, bool $count): array
    {
        $this->logDebug("Build query filter of User consent purposes");
        $span = $this->startSpan("Build query filter of User consent purposes");
        try {
            $join = '';
            $query = '';
            $params = [];
            $order = '';
            $limit = '';
            if ($filter) {
                $filterUids = $filter->uids();
                if (null !== $filterUids && count($filterUids) > 1) {
                    $query .= ' and "access_user_consent_purposes"."uid" in (:uids)';
                    $params[] = new SqlParam(name:'uids', value: $filterUids, type: SqlParam::STR);
                } elseif (null !== $filterUids) {
                    $query .= ' and "access_user_consent_purposes"."uid" = :uid';
                    $params[] = new SqlParam(name:'uid', value: $filterUids[0], type: SqlParam::STR);
                }
                $filterSearch = $filter->search();
                if (null !== $filterSearch) {
                    $query .= ' and ( "access_user_consent_purposes"."uid" like :search)';
                    $params[] = new SqlParam(name:'search', value: '%'. $filterSearch . '%', type: SqlParam::STR);
                }
                $filterUser = $filter->user();
                if (null !== $filterUser) {
                    $query .= ' and "access_user_consent_purposes"."user" = :user ';
                    $params[] = new SqlParam(name: 'user', value: $filterUser->uid(), type: SqlParam::STR);
                }
                $filterUsers = $filter->users();
                if (null !== $filterUsers) {
                    $query .= ' and "access_user_consent_purposes"."user" in (:users)  ';
                    $params[] = new SqlParam(name: 'users', value: $filterUsers, type: SqlParam::STR);
                }
                $filterConsentPurpose = $filter->consentPurpose();
                if (null !== $filterConsentPurpose) {
                    $query .= ' and "access_user_consent_purposes"."consent_purpose" = :consentPurpose ';
                    $params[] = new SqlParam(name: 'consentPurpose', value: $filterConsentPurpose->uid(), type: SqlParam::STR);
                }
                $filterConsentPurposes = $filter->consentPurposes();
                if (null !== $filterConsentPurposes) {
                    $query .= ' and "access_user_consent_purposes"."consent_purpose" in (:consentPurposes)  ';
                    $params[] = new SqlParam(name: 'consentPurposes', value: $filterConsentPurposes, type: SqlParam::STR);
                }
                $filterUserTenantTenantAccesible = $filter->userTenantTenantAccesible();
                if (null !== $filterUserTenantTenantAccesible) {
                    $join .= ' LEFT JOIN "access_user" as "userTenantTenantAccesibleUser" ON "userTenantTenantAccesibleUser"."uid" = "access_user_consent_purposes"."user" LEFT JOIN "access_tenant" as "userTenantTenantAccesibleTenant" ON "userTenantTenantAccesibleTenant"."uid" = "userTenantTenantAccesibleUser"."tenant"';
                    $query .= ' and "userTenantTenantAccesibleTenant"."uid" = :userTenantTenantAccesible';
                    $params[] = new SqlParam(name: 'userTenantTenantAccesible', value: $filterUserTenantTenantAccesible, type: SqlParam::STR);
                }
            }
            if ($sort) {
                $sortLimit = $sort->limit();
                if ($sortLimit) {
                    $limit = ' LIMIT ' . $this->db->escapeValue($sortLimit, SqlParam::INT);
                }
                $sortSinceUid = $sort->sinceUid();
                if (null !== $sortSinceUid) {
                    $query .= ' and  "uid" < :sinceUid';
                    $params[] = new SqlParam(name: 'sinceUid', value: $sortSinceUid, type: SqlParam::STR);
                }
                $order = ', "access_user_consent_purposes"."uid" desc';
            }
            return [
              'query' => 'SELECT '.($count ? ' count("access_user_consent_purposes".*) as count ' : '"access_user_consent_purposes".*').' FROM "access_user_consent_purposes"'
                . $join
                . ($query ? ' WHERE ' . substr($query, 4) : '')
                . ($order ? ' ORDER BY ' . substr($order, 2) : '') . $limit,
              'params' => $params];
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    private function mapper(array $row): UserConsentPurposes
    {
        $this->logDebug("Mapping from sql to entity for User consent purposes");
        $span = $this->startSpan("Mapping from sql to entity for User consent purposes");
        try {
            $uid = $row['uid'] ?? null;
            if (null === $uid) {
                throw ConstraintException::ofError('not-null', ['uid'], [null]);
            }
            $rawUser = $row['user'] ?? null;
            if (null === $rawUser) {
                throw ConstraintException::ofError('not-null', ['user'], [null]);
            }
            $user = new UserRef(uid: $rawUser);
            $rawConsentPurpose = $row['consent_purpose'] ?? null;
            if (null === $rawConsentPurpose) {
                throw ConstraintException::ofError('not-null', ['consentPurpose'], [null]);
            }
            $consentPurpose = new ConsentPurposeRef(uid: $rawConsentPurpose);
            $granted = isset($row['granted']) ? !! $row['granted'] : null;
            if (null === $granted) {
                throw ConstraintException::ofError('not-null', ['granted'], [null]);
            }
            $decisionAt = $row['decision_at'] ? new \DateTimeImmutable($row['decision_at']) : null;
            $ipAddress = $row['ip_address'] ?? null;
            $userAgent = $row['user_agent'] ?? null;
            $version = $row['version'] ?? null;
            return new UserConsentPurposes(
                uid: $uid,
                user: $user,
                consentPurpose: $consentPurpose,
                granted: $granted,
                decisionAt: $decisionAt,
                ipAddress: $ipAddress,
                userAgent: $userAgent,
                version: $version,
            );
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\TenantLoginProvider\Application\Usecase\UploadSamlIdpIdpCert;

use Psr\EventDispatcher\EventDispatcherInterface;
use Throwable;
use Civi\Lughauth\Shared\Security\Allow;
use Civi\Lughauth\Shared\Connector\FileStorage\BinaryContent;
use Civi\Lughauth\Shared\Exception\UnauthorizedException;
use Civi\Lughauth\Features\Access\TenantLoginProvider\Application\Service\Visibility\TenantLoginProviderVisibilityService;
use Civi\Lughauth\Features\Access\TenantLoginProvider\Domain\Gateway\TenantLoginProviderWriteGateway;
use Civi\Lughauth\Shared\Observability\LoggerAwareTrait;
use Civi\Lughauth\Shared\Observability\TracerAwareTrait;

class TenantLoginProviderUploadSamlIdpIdpCertUsecase
{
    use LoggerAwareTrait;
    use TracerAwareTrait;

    public function __construct(
        private readonly EventDispatcherInterface $dispatcher,
        private readonly TenantLoginProviderVisibilityService $visibility,
        private readonly TenantLoginProviderWriteGateway $writer
    ) {
    }

    public function allowUpload(): Allow
    {
        $this->logDebug("Check allow of temp upload SamlIdpIdpCert usecase for Tenant login provider");
        $span = $this->startSpan("Check allow of temp upload SamlIdpIdpCert usecase for Tenant login provider");
        try {
            $result = $this->dispatcher->dispatch(new TenantLoginProviderUploadSamlIdpIdpCertAllowDecision(Allow::allowed('upload', 'Allowed to upload by default')));
            return $result->getAllow();
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function readTemporal(string $query): BinaryContent
    {
        $this->logDebug("Run retrieve temp upload SamlIdpIdpCert usecase for Tenant login provider");
        $span = $this->startSpan("Run retrieve temp upload SamlIdpIdpCert usecase for Tenant login provider");
        try {
            $allow = $this->allowUpload();
            if (!$allow->allowed) {
                throw new UnauthorizedException($allow->reason ?? 'Not allowed to read tempora upload for Tenant login provider');
            }
            return $this->writer->readTemporalSamlIdpIdpCert($query);
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
    public function temporalStore(BinaryContent $binary): string
    {
        $this->logDebug("Run temp upload SamlIdpIdpCert usecase for Tenant login provider");
        $span = $this->startSpan("Run temp upload SamlIdpIdpCert usecase for Tenant login provider");
        try {
            $allow = $this->allowUpload();
            if (!$allow->allowed) {
                throw new UnauthorizedException($allow->reason ?? 'Not allowed to upload store on Tenant login provider');
            }
            $this->dispatcher->dispatch(new TenantLoginProviderUploadSamlIdpIdpCertCheck($binary));
            $input = $this->dispatcher->dispatch(new TenantLoginProviderUploadSamlIdpIdpCertEnrich($binary, $binary));
            return $this->writer->temporalStoreSamlIdpIdpCert($input->getResult());
        } catch (Throwable $ex) {
            $span->recordException($ex);
            throw $ex;
        } finally {
            $span->end();
        }
    }
}

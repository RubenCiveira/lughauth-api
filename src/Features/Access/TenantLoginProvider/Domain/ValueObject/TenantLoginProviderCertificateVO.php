<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\TenantLoginProvider\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;

class TenantLoginProviderCertificateVO
{
    public static function empty(): TenantLoginProviderCertificateVO
    {
        return new TenantLoginProviderCertificateVO(null);
    }
    public static function from(TenantLoginProviderCertificateVO|string|null $value): TenantLoginProviderCertificateVO
    {
        if (is_a($value, TenantLoginProviderCertificateVO::class)) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            return $candidate;
        }
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?TenantLoginProviderCertificateVO
    {
        if (is_a($value, TenantLoginProviderCertificateVO::class)) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new TenantLoginProviderCertificateVO($value);
        } elseif (is_string($value)) {
            return new TenantLoginProviderCertificateVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['certificate'], [$value], ['string']));
            return null;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?string $certificate
    ) {
    }
    public function value(): ?string
    {
        return $this->certificate;
    }
}

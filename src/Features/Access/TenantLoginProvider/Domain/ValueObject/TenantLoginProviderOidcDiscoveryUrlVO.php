<?php

/* @autogenerated */
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\TenantLoginProvider\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;
use Civi\Lughauth\Shared\Value\Validation\Rule\Length;

class TenantLoginProviderOidcDiscoveryUrlVO
{
    public static function empty(): TenantLoginProviderOidcDiscoveryUrlVO
    {
        return new TenantLoginProviderOidcDiscoveryUrlVO(null);
    }
    public static function from(TenantLoginProviderOidcDiscoveryUrlVO|string|null $value): TenantLoginProviderOidcDiscoveryUrlVO
    {
        return self::fromUnsafe($value);
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?TenantLoginProviderOidcDiscoveryUrlVO
    {
        if ($value instanceof TenantLoginProviderOidcDiscoveryUrlVO) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new TenantLoginProviderOidcDiscoveryUrlVO($value);
        } elseif (is_string($value)) {
            // If is a ValueObject, we need to append all the errors for the context
            $valid = true;
            foreach (self::rules() as $rule) {
                if ($fail = $rule->check($value)) {
                    $list->add(ConstraintFail::fromRuleFail('oidcDiscoveryUrl', $fail));
                    $valid = false;
                }
            }
            return $valid ? new TenantLoginProviderOidcDiscoveryUrlVO($value) : null;
        } else {
            $list->add(new ConstraintFail('wrong_type', ['oidcDiscoveryUrl'], [$value], ['string']));
            return null;
        }
    }
    public static function rules(): array
    {
        return [
          new Length(min: null, max: 250),
        ];
    }
    private static function fromUnsafe(mixed $value): TenantLoginProviderOidcDiscoveryUrlVO
    {
        if ($value instanceof TenantLoginProviderOidcDiscoveryUrlVO) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            $candidate = self::tryFrom($value, $errorsList);
            if ($errorsList->hasErrors()) {
                throw $errorsList->asConstraintException();
            }
            \assert($candidate instanceof TenantLoginProviderOidcDiscoveryUrlVO);
            return $candidate;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?string $oidcDiscoveryUrl
    ) {
    }
    public function value(): ?string
    {
        return $this->oidcDiscoveryUrl;
    }
    public function equals(?TenantLoginProviderOidcDiscoveryUrlVO $other): bool
    {
        return $this->value() == $other?->value();
    }
}

<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\TenantLoginProvider\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;

class TenantLoginProviderDirectAccessVO
{
    public static function empty(): TenantLoginProviderDirectAccessVO
    {
        return new TenantLoginProviderDirectAccessVO(null);
    }
    public static function from(TenantLoginProviderDirectAccessVO|bool|null $value): TenantLoginProviderDirectAccessVO
    {
        if (is_a($value, TenantLoginProviderDirectAccessVO::class)) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            return self::tryFrom($value, $errorsList);
        }
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?TenantLoginProviderDirectAccessVO
    {
        if (is_a($value, TenantLoginProviderDirectAccessVO::class)) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new TenantLoginProviderDirectAccessVO($value);
        } elseif (is_bool($value)) {
            return new TenantLoginProviderDirectAccessVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['directAccess'], [$value], ['bool']));
            return null;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?bool $directAccess
    ) {
    }
    public function value(): ?bool
    {
        return $this->directAccess;
    }
}

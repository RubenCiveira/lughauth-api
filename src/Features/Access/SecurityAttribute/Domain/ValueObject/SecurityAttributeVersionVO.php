<?php

# @autogenerated
declare(strict_types=1);

namespace Civi\Lughauth\Features\Access\SecurityAttribute\Domain\ValueObject;

use Civi\Lughauth\Shared\Value\Validation\ConstraintFailList;
use Civi\Lughauth\Shared\Value\Validation\ConstraintFail;

class SecurityAttributeVersionVO
{
    public static function empty(): SecurityAttributeVersionVO
    {
        return new SecurityAttributeVersionVO(null);
    }
    public static function from(SecurityAttributeVersionVO|int|null $value): SecurityAttributeVersionVO
    {
        if (is_a($value, SecurityAttributeVersionVO::class)) {
            // If is a ValueObject, its already validated
            return $value;
        } else {
            // If is not a ValueObject, validation is need and exception throw
            $errorsList = new ConstraintFailList();
            return self::tryFrom($value, $errorsList);
        }
    }
    public static function tryFrom(mixed $value, ConstraintFailList $list): ?SecurityAttributeVersionVO
    {
        if (is_a($value, SecurityAttributeVersionVO::class)) {
            // If is a ValueObject, its already validated... nothing to append
            return $value;
        } elseif (!$value) {
            return new SecurityAttributeVersionVO($value);
        } elseif (is_integer($value)) {
            return new SecurityAttributeVersionVO($value);
        } else {
            $list->add(new ConstraintFail('wrong_type', ['version'], [$value], ['int']));
            return null;
        }
    }
    /**
     * private constructor to avoid build a value without all the rule validations.
     */
    private function __construct(
        private readonly ?int $version
    ) {
    }
    public function value(): ?int
    {
        return $this->version;
    }
}

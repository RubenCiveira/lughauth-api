<?php

# @autogenerated
declare(strict_types=1);

use Civi\Lughauth\Features\Access\TrustedClient\Domain\TrustedClientRef;
use Civi\Lughauth\Features\Access\RelyingParty\Domain\RelyingPartyRef;
use PHPUnit\Framework\TestCase;
use Civi\Lughauth\Features\Access\SecurityAttribute\Domain\SecurityAttribute;

final class SecurityAttributeUnitTest extends TestCase
{
    public function test_init_store_values(): void
    {
        // @Arrange
        $one = new SecurityAttribute(
            uid: 'one',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['one'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['one'])->getMock(),
            resource: 'one',
            attribute: 'one',
            enabled: true,
            readVisibility: 'PUBLIC',
            writeVisibility: 'PUBLIC',
            version: 1,
        );

        // @Act
        $other = $one->toAttributes()->build();

        // @Assert
        $this->assertEquals($one->uid(), $other->uid());
        $this->assertEquals($one->getTrustedClient(), $other->getTrustedClient());
        $this->assertTrue($one->isTrustedClientChanged());
        $this->assertEquals($one->getRelyingParty(), $other->getRelyingParty());
        $this->assertTrue($one->isRelyingPartyChanged());
        $this->assertEquals($one->getResource(), $other->getResource());
        $this->assertTrue($one->isResourceChanged());
        $this->assertEquals($one->getAttribute(), $other->getAttribute());
        $this->assertTrue($one->isAttributeChanged());
        $this->assertEquals($one->getReadVisibility(), $other->getReadVisibility());
        $this->assertTrue($one->isReadVisibilityChanged());
        $this->assertEquals($one->getWriteVisibility(), $other->getWriteVisibility());
        $this->assertTrue($one->isWriteVisibilityChanged());
        $this->assertEquals($one->getVersion(), $other->getVersion());
        $this->assertTrue($one->isVersionChanged());
    }
    public function test_replace_change_values(): void
    {
        // @Arrange
        $base = new SecurityAttribute(
            uid: 'one',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['one'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['one'])->getMock(),
            resource: 'one',
            attribute: 'one',
            enabled: true,
            readVisibility: 'PUBLIC',
            writeVisibility: 'PUBLIC',
            version: 1,
        );
        $other = new SecurityAttribute(
            uid: 'other',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['other'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['other'])->getMock(),
            resource: 'other',
            attribute: 'other',
            enabled: false,
            readVisibility: 'AUTHORIZED',
            writeVisibility: 'AUTHORIZED',
            version: 2,
        );

        // @Act
        $one = $base->replace($other->toAttributes());

        // @Assert
        $this->assertEquals($one->uid(), $base->uid());
        $this->assertEquals($one->getTrustedClient(), $other->getTrustedClient());
        $this->assertTrue($one->isTrustedClientChanged($base));
        $this->assertEquals($one->getRelyingParty(), $other->getRelyingParty());
        $this->assertTrue($one->isRelyingPartyChanged($base));
        $this->assertEquals($one->getResource(), $other->getResource());
        $this->assertTrue($one->isResourceChanged($base));
        $this->assertEquals($one->getAttribute(), $other->getAttribute());
        $this->assertTrue($one->isAttributeChanged($base));
        $this->assertEquals($one->getReadVisibility(), $other->getReadVisibility());
        $this->assertTrue($one->isReadVisibilityChanged($base));
        $this->assertEquals($one->getWriteVisibility(), $other->getWriteVisibility());
        $this->assertTrue($one->isWriteVisibilityChanged($base));
        $this->assertEquals($one->getVersion(), $other->getVersion());
        $this->assertTrue($one->isVersionChanged($base));
    }
    public function test_create_store_values(): void
    {
        // @Arrange
        $one = new SecurityAttribute(
            uid: 'one',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['one'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['one'])->getMock(),
            resource: 'one',
            attribute: 'one',
            enabled: true,
            readVisibility: 'PUBLIC',
            writeVisibility: 'PUBLIC',
            version: 1,
        );

        // @Act
        $other = SecurityAttribute::create($one->toAttributes());

        // @Assert
        $this->assertEquals($one->uid(), $other->uid());
        $this->assertEquals($one->getTrustedClient(), $other->getTrustedClient());
        $this->assertTrue($one->isTrustedClientChanged());
        $this->assertEquals($one->getRelyingParty(), $other->getRelyingParty());
        $this->assertTrue($one->isRelyingPartyChanged());
        $this->assertEquals($one->getResource(), $other->getResource());
        $this->assertTrue($one->isResourceChanged());
        $this->assertEquals($one->getAttribute(), $other->getAttribute());
        $this->assertTrue($one->isAttributeChanged());
        $this->assertEquals($one->getReadVisibility(), $other->getReadVisibility());
        $this->assertTrue($one->isReadVisibilityChanged());
        $this->assertEquals($one->getWriteVisibility(), $other->getWriteVisibility());
        $this->assertTrue($one->isWriteVisibilityChanged());
        $this->assertEquals($one->getVersion(), $other->getVersion());
        $this->assertTrue($one->isVersionChanged());
        $this->assertCount(0, $one->getTheEvents());
        $this->assertCount(1, $other->getTheEvents());
    }
    public function test_update_change_values(): void
    {
        $base = new SecurityAttribute(
            uid: 'one',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['one'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['one'])->getMock(),
            resource: 'one',
            attribute: 'one',
            enabled: true,
            readVisibility: 'PUBLIC',
            writeVisibility: 'PUBLIC',
            version: 1,
        );
        $other = new SecurityAttribute(
            uid: 'other',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['other'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['other'])->getMock(),
            resource: 'other',
            attribute: 'other',
            enabled: false,
            readVisibility: 'AUTHORIZED',
            writeVisibility: 'AUTHORIZED',
            version: 2,
        );

        // @Act
        $one = $base->update($other->toAttributes());

        // @Assert
        $this->assertEquals($one->uid(), $base->uid());
        $this->assertEquals($one->getTrustedClient(), $other->getTrustedClient());
        $this->assertTrue($one->isTrustedClientChanged($base));
        $this->assertEquals($one->getRelyingParty(), $other->getRelyingParty());
        $this->assertTrue($one->isRelyingPartyChanged($base));
        $this->assertEquals($one->getResource(), $other->getResource());
        $this->assertTrue($one->isResourceChanged($base));
        $this->assertEquals($one->getAttribute(), $other->getAttribute());
        $this->assertTrue($one->isAttributeChanged($base));
        $this->assertEquals($one->getReadVisibility(), $other->getReadVisibility());
        $this->assertTrue($one->isReadVisibilityChanged($base));
        $this->assertEquals($one->getWriteVisibility(), $other->getWriteVisibility());
        $this->assertTrue($one->isWriteVisibilityChanged($base));
        $this->assertEquals($one->getVersion(), $other->getVersion());
        $this->assertTrue($one->isVersionChanged($base));
        $this->assertCount(0, $other->getTheEvents());
        $this->assertCount(1, $one->getTheEvents());
    }
    public function test_delete_store_values(): void
    {
        // @Arrange
        $one = new SecurityAttribute(
            uid: 'one',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['one'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['one'])->getMock(),
            resource: 'one',
            attribute: 'one',
            enabled: true,
            readVisibility: 'PUBLIC',
            writeVisibility: 'PUBLIC',
            version: 1,
        );

        // @Act
        $other = $one->delete();

        // @Assert
        $this->assertEquals($one->uid(), $other->uid());
        $this->assertEquals($one->getTrustedClient(), $other->getTrustedClient());
        $this->assertTrue($one->isTrustedClientChanged());
        $this->assertEquals($one->getRelyingParty(), $other->getRelyingParty());
        $this->assertTrue($one->isRelyingPartyChanged());
        $this->assertEquals($one->getResource(), $other->getResource());
        $this->assertTrue($one->isResourceChanged());
        $this->assertEquals($one->getAttribute(), $other->getAttribute());
        $this->assertTrue($one->isAttributeChanged());
        $this->assertEquals($one->getReadVisibility(), $other->getReadVisibility());
        $this->assertTrue($one->isReadVisibilityChanged());
        $this->assertEquals($one->getWriteVisibility(), $other->getWriteVisibility());
        $this->assertTrue($one->isWriteVisibilityChanged());
        $this->assertEquals($one->getVersion(), $other->getVersion());
        $this->assertTrue($one->isVersionChanged());
    }
    public function test_disable_modify(): void
    {
        // @Arrange
        $source = new SecurityAttribute(
            uid: 'one',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['one'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['one'])->getMock(),
            resource: 'one',
            attribute: 'one',
            enabled: true,
            readVisibility: 'PUBLIC',
            writeVisibility: 'PUBLIC',
            version: 1,
        );
        $sourceEnabled = true;
        $targetEnabled = false;
        $source = $source->withEnabled($sourceEnabled);

        // @Act
        $target = $source->disable();

        // @Assert
        $this->assertEquals(true, $source->getEnabled());
        $this->assertEquals($targetEnabled, $target->getEnabled());
        $this->assertNotEquals($sourceEnabled, $target->getEnabled());
    }
    public function test_enable_modify(): void
    {
        // @Arrange
        $source = new SecurityAttribute(
            uid: 'one',
            trustedClient: $this->getMockBuilder(TrustedClientRef::class)->setConstructorArgs(['one'])->getMock(),
            relyingParty: $this->getMockBuilder(RelyingPartyRef::class)->setConstructorArgs(['one'])->getMock(),
            resource: 'one',
            attribute: 'one',
            enabled: true,
            readVisibility: 'PUBLIC',
            writeVisibility: 'PUBLIC',
            version: 1,
        );
        $sourceEnabled = false;
        $targetEnabled = true;
        $source = $source->withEnabled($sourceEnabled);

        // @Act
        $target = $source->enable();

        // @Assert
        $this->assertEquals(false, $source->getEnabled());
        $this->assertEquals($targetEnabled, $target->getEnabled());
        $this->assertNotEquals($sourceEnabled, $target->getEnabled());
    }
}
